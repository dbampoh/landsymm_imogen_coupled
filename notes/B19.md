# B19 — Closed-loop coupled-pipeline verification (LandSyMM ↔ intermediary_py ↔ LPJ-GUESS ↔ IMOGEN)

**Date opened:** 2026-05-16 morning (session 5)
**Working branch:** `b19-pipeline-verification` (off `main @ v0.17.8-step17c-prep-complete` commit `56fcfd8`)
**Status:** 🔧 **OPEN — Phase 0 DONE (this commit, 2026-05-16); Phase 1 NEXT**
**Phase progress at this commit:** Phase 0 ✅ outcome (β) hardcoding fix landed at `lpjguess/modules/imogenoutput.cpp:341-401` (+54/-12 LOC; 3 substantive LOC + ~45-LOC inline-comment rewrite + 5-LOC dprintf observability); see §2.5 landing record + §11 index.
**Estimated effort:** ~2.5-4 d essential (revised down from 3-7 d at 17c.0.7 introduction after 2026-05-15 night code-reality check confirmed steps 10/11/12/13/14 are all already DONE per `EXECUTION_PLAN.md`); Phase 0 actual ~3 h (within §2.3 estimate of 2.5-3 h) ✓
**Successor:** B20 (literature-comparison sanity check; ~1-2 d recommended), then 17c.1+ cluster phases on KIT IMK-IFU `owl`
**Predecessor:** Step 17c.0 PREP arc (11/11 sub-phases COMPLETE; tag `v0.17.8-step17c-prep-complete`)

---

## 0. Document purpose + status + decisions baked in

This document is the canonical work-plan + landing-record reference for B19, the essential closed-loop coupled-pipeline verification milestone that bridges the PREP arc and the cluster phases. Analogous to `notes/STEP_17c.md` for the 17c.0 PREP arc, it is intended to be:

1. **Live-updated** as phases land (each phase ends with a "landing record" sub-section analogous to STEP_17c §1.X.Y).
2. **Cross-referenced** from `notes/FOLLOWUPS.md` B19 + B20 entries, `notes/STEP_17c.md` §1.7.8, `CHANGELOG.md`, `EXECUTION_PLAN.md` row 19 (or new B19-specific row), and `_chat_artifacts/CHAT_HANDOFF_*` chat artifacts.
3. **The single source of truth** for B19 phase boundaries, acceptance criteria, and design decisions (so future doc-cascade edits can cite this file as the canonical statement rather than recapitulating).

**Decisions baked in from session-5 opening (2026-05-16 morning; per assistant Q1/Q2/Q3 + user "I leave this to you to decide ... as long as you are meticulous, systematic, and comprehensive"):**

- **Q1 → BEFORE**: SPINUP/FIRSTCALL hardcoding investigation (Phase 0) runs BEFORE closed-loop validation (Phase 4) to pre-empt confounders.
- **Q2 → DEFER-TO-EMPIRICS WITH MIXED-TOLERANCE FALLBACK**: Phase 4 closed-loop validation runs first with NO tolerance gate (reporting-only); tolerance is locked in after observing the empirical drift envelope, with strict (≤1%) on GHG concentrations + loose (≤5%) on intermediate climate fields as the fallback policy if drift is uneven across field types.
- **Q3 → RETAIN FULL**: Phase 1 re-runs `intermediary_py` end-to-end + the step-13 adapter on fresh checkout as a reproducibility gate; byte-equality vs the checked-in outputs at `intermediary_py/imogen_ghg_controller/outputs/imogen_inputs/imogen_inputs_<SSP>.csv` is the PASS criterion.

---

## 1. B19 scope

### 1.1 The 8-stage coupled pipeline — reality check at session-5 opening

The full LandSyMM ↔ intermediary_py ↔ LPJ-GUESS ↔ IMOGEN coupled pipeline consists of 8 stages (per `EXECUTION_PLAN.md` V.1):

| Stage | What it does | LandSyMM project step | Status at session-5 opening | B19-relevance |
|---|---|---|---|---|
| 1 | PLUMv2 land-use + activity-data outputs | Step 10 (data acquisition) | ✅ DONE | input to stage 2 |
| 2 | `intermediary_py` end-to-end (RCMIP-substituted emissions trajectories per scenario) | Step 11 (intermediary_py end-to-end) | ✅ DONE; outputs at `intermediary_py/imogen_ghg_controller/outputs/imogen_inputs/imogen_inputs_SSP*.csv` (5 SSP + 1 long) | **B19-Phase 1** reproducibility re-run |
| 3 | Step-13 adapter (wide CSV → 4 narrow ASCII files) | Step 13 (adapter) | ✅ DONE; tested at step 13 + wired into `imogen_intermediary.ins` Option B | **B19-Phase 1** reproducibility re-run |
| 4 | IMOGEN engine state-machine inputs verification (SPINUP/FIRSTCALL/handshake files) | Step 8 (LPJG-side handshake writer); step 7 (IMOGEN-side polling loop F-4 fix) | ✅ DONE for default; **SPINUP/FIRSTCALL hardcoded** at `lpjguess/modules/imogenoutput.cpp:336-364` (commit `a543e9d` step 8); explicit inline rationale at lines 342-350 | **B19-Phase 0** investigation; **B19-Phase 2** integration verification |
| 5 | LPJG `coupling_mode = prescribed` run consuming adapter outputs | Step 14 (`scripts/run_coupled.sh` workstation launcher) | ✅ DONE for default smoke + production scaffolding | **B19-Phase 2** wire production .ins; **B19-Phase 3** execute round-trip |
| 6 | IMOGEN Fortran engine round-trip (consumes LPJG handshake + adapter anthro emissions; emits `CO2.dat` with CH4 + N2O concentrations as cols 7+8) | Step 17 (F-12 engine-level integration) | 🔧 **NOT YET EXERCISED end-to-end on workstation** | **B19-Phase 3** the actual scientific milestone |
| 7 | Closed-loop comparison: round-trip-output GHG concentrations + climate anomalies vs legacy A/B references | Step 18 (closed-loop validation; NEW B19) | 🔧 **NOT YET DONE** | **B19-Phase 4** scientific validation |
| 8 | `CO2.dat` engine output ready for downstream consumption | Step 19 (operational packaging) | partially done at step 14 (output dirs exist; not yet exercised) | covered by Phase 3+4 |

**Net assessment**: stages 1+2+3+5+8 are DONE at session-5 opening per the PREP arc + steps 10/11/12/13/14; stages 4+6+7 are the substantive B19 work surface. Phase 0 investigates the hardcoding nuance in stage 4; Phase 1 re-verifies stages 2+3 reproducibility; Phase 2 verifies stage 4+5 integration; Phase 3 executes stage 6 end-to-end; Phase 4 executes stage 7.

### 1.2 B19 essential sub-items vs optional polish (cross-ref `notes/FOLLOWUPS.md` B19 entry)

The B19 entry in `notes/FOLLOWUPS.md` enumerates 9 sub-items (a)-(i). The 2026-05-15 night code-reality check classified them by essentiality:

| Sub-item | Description | Essentiality | B19 phase mapping |
|---|---|---|---|
| (a) | `intermediary_py` end-to-end documentation cross-link to step 11 | OPTIONAL (already done at step 11 close-out) | none (covered by Phase 1 ref) |
| (b) | step-13 adapter regression run | OPTIONAL (already done at step 13 close-out) | covered by Phase 1 |
| (c) | **IMOGEN engine round-trip end-to-end (workstation)** | **ESSENTIAL** | **Phase 3** |
| (d) | engine output `CO2.dat` schema verification + CH4 + N2O column presence | OPTIONAL (will be exercised as side-effect of Phase 3) | covered by Phase 3 |
| (e) | **closed-loop validation: engine output vs legacy A/B reference** | **ESSENTIAL** | **Phase 4** |
| (f) | engine-side defensive observability: explicit `[ERROR] could not open handshake file at <path>` if `DIR_COMMON` is empty (per 17c.0.7 §3.10.6 Path α follow-up) | OPTIONAL (harness-layer fix already lands at 17c.0.7) | DEFERRED to B19 Phase 5 sub-task or post-B19 polish |
| (g) | production-gridlist exercise (62892-cell SSP1-2.6) | OPTIONAL (smoke gridlist sufficient for v1.0 closed-loop verification; production-gridlist exercise belongs in cluster phases 17c.1+) | DEFERRED to 17c.1+ |
| (h) | per-SSP scenario sweep (all 5 SSPs) | OPTIONAL (one scenario sufficient for v1.0 closed-loop verification) | DEFERRED to 17c.1+ |
| (i) | **`imogen_lpjg.txt` SPINUP/FIRSTCALL hardcoding investigation** | **ESSENTIAL (per Q1 ordering: BEFORE Phase 4)** | **Phase 0** |

**Essential sub-items: (c) + (e) + (i)** — 3/9. The rest are optional polish covered as side-effects or deferred to cluster phases / post-B19.

### 1.3 The 5-phase B19 work-plan at a glance

| Phase | Title | Est. effort | Maps to sub-item(s) | Pass criterion |
|---|---|---|---|---|
| **0** | SPINUP/FIRSTCALL hardcoding investigation | ~0.5 d (actual: ~3 h ✓) | (i) ✅ CLOSED at this commit | ✅ DONE — outcome (β) hardcoding fix landed; 3-LOC source change at `imogenoutput.cpp:399-401` + ~45-LOC inline-comment rewrite + 5-LOC dprintf; landing record at §2.5 |
| **1** | `intermediary_py` + step-13 adapter reproducibility re-run | ~30-60 min wall + ~0.5 d analysis | (a) + (b) cross-ref | byte-equality vs checked-in outputs OR documented & explained drift |
| **2** | IMOGEN engine state-machine prep + inputs verification | ~0.5 d | (parts of c) | handshake-file write-paths confirmed; .ins file ready; engine binary exists; bootstrap files in place |
| **3** | IMOGEN engine round-trip on workstation (smoke 4-cell, 2-year) | ~0.5-1 d | (c) + (d) | engine completes WITHOUT F-10 deadlock; `CO2.dat` emitted with 8+ columns; per-year flush records exist |
| **4** | Closed-loop validation vs legacy A/B (no-gate first per Q2δ) | ~0.5-1 d | (e) | empirical drift envelope characterized; tolerance locked in; PASS recorded |
| **5** | Doc cascade + B19 close-out tag | ~0.5 d | bookkeeping | 6-surface in-tree cascade complete; tag `v0.19.0-b19-closed-loop-verified` (or similar); 3-remote converged |
| | **TOTAL** | **~2.5-4 d** | | |

---

## 2. Phase 0 — SPINUP/FIRSTCALL hardcoding investigation

### 2.1 Scope + acceptance + outcomes

**Scope**: Investigate whether the SPINUP=FALSE + FIRSTCALL=FALSE hardcoding at `lpjguess/modules/imogenoutput.cpp:336-364` (the `imogen_lpjg.txt` writer in `ImogenOutput::flush_year`) is correct for v1.0 single-process and v1.0+MPI multi-rank modes, OR whether it represents a latent defect that needs to be replaced with dynamic state-machine logic reading from ins-file parameters (`FIRSTHISTYEAR`, `IYEND`) per the existing inline-comment caveat at lines 344-347.

**Acceptance criteria for Phase 0** (one of three outcomes):

- **(α) HARDCODING IS CORRECT** — verify (a) the F-4 fix at step 7 makes FIRSTCALL=FALSE safe for year 2+ as the inline comment at lines 348-350 claims; (b) the conservative SPINUP=FALSE is appropriate for v1.0 (per the inline rationale at lines 342-347, "we conservatively flag every year as non-spinup ... so the IMOGEN engine just advances year-by-year"); (c) legacy A's `imogen_lpjg.txt` files exhibit consistent values (or, if they differ in legacy, the legacy semantics are reproduced or explicitly different by design). Document the rationale in this file's Phase 0 landing record + tighten the inline comment at `imogenoutput.cpp:342-350` if anything is unclear. **Decision**: close Phase 0 with NO code change.

- **(β) HARDCODING IS WRONG** — fix by plumbing `FIRSTHISTYEAR` / `IYEND` through the `imogen_intermediary.ins` file (~5-20 LOC: extend `IMOGENConfig` namespace, register the parameters via `declare_parameter`, and compute SPINUP/FIRSTCALL dynamically in `flush_year`); write a small assertion/test exercising the year-2/year-N transition; commit. **Decision**: close Phase 0 with code change + test.

- **(γ) INCONCLUSIVE** — Phase 0 cannot resolve without exercising the IMOGEN engine round-trip (Phase 3). Document the inconclusiveness here + Phase 4 acceptance criteria gets a noted-deferred subtask for "verify SPINUP/FIRSTCALL semantics from Phase 3 round-trip data". **Decision**: close Phase 0 with a documented deferral to Phase 4.

### 2.2 Evidence we have today (before Phase 0 starts)

| Anchor | Value | Source |
|---|---|---|
| Hardcoding landed at | commit `a543e9d` step 8 (2026-05-06 20:32:13 +0200) | `git blame -L 336,364 lpjguess/modules/imogenoutput.cpp` |
| Hardcoding line range | `imogenoutput.cpp:336-364` (the `imogen_lpjg.txt` ofstream block) | inline read |
| Hardcoded values | `SPINUP FALSE` + `KEEPRUNNING TRUE` + `FIRSTCALL FALSE` | `imogenoutput.cpp:357-359` |
| Inline rationale | lines 342-350 (8 LOC comment block) | inline read |
| Self-noted limitation | "For genuine spin-up phase detection we'd need the first-historic-year and run-end year; that would be reading from FIRSTHISTYEAR / IYEND ins parameters which requires plumbing through the ins-file which is out of step-8 scope." | `imogenoutput.cpp:344-347` |
| Self-noted FIRSTCALL safety claim | "The IMOGEN-side polling loop's FIRSTCALL handling is fine with FIRSTCALL=FALSE for year 2+ since the F-4 fix at step 7 makes the first-call special case not needed." | `imogenoutput.cpp:348-350` |
| Bootstrap file (different values) | `runs/<SSP>/Common-directory/LPJG_main/IMOGEN/imogen_lpjg.txt` initial write: `SPINUP FALSE / KEEPRUNNING TRUE / FIRSTCALL TRUE` (FIRSTCALL=TRUE for engine's very first iteration before `flush_year` overwrites) | `scripts/run_coupled.sh:317-326` |
| Bootstrap file purpose | "In v1.0 single-process mode, the IMOGEN engine's polling loop (step 7's restored bug C2/C3 fixes) requires imogen_lpjg.txt + done to exist before the engine's first iteration. LPJG can't supply them yet because LPJG's main loop hasn't started (init() hasn't returned). So we pre-populate these as bootstrap files." | `scripts/run_coupled.sh:308-313` comments |
| F-4 fix at step 7 location | `notes/STEP_7.md` — IMOGEN engine polling-loop bug fixes (FIRSTCALL handling) | needs verification step in Phase 0 |
| Legacy A `imogen_lpjg.txt` example | (not yet inspected; needs Phase 0 step-2a to locate via `_chat_artifacts/legacy_runs/A/` or analogous) | TBD in Phase 0 |

### 2.3 Phase 0 investigation steps

| Step | Description | Anticipated time |
|---|---|---|
| **2.3.1** | Read `notes/STEP_7.md` + relevant commit's `lpjguess/modules/imogencfx.cpp` polling-loop section to confirm the F-4 fix indeed makes FIRSTCALL=FALSE safe for year 2+ (the inline comment's safety claim). Specifically: trace the FIRSTCALL=FALSE code path through the engine to confirm there's no state-machine branch that fails on it. | ~30 min |
| **2.3.2** | Locate + read legacy A's `imogen_lpjg.txt` file(s) from a representative legacy A run. Compare SPINUP/FIRSTCALL values across consecutive years to confirm legacy semantics. (Search likely paths: `_chat_artifacts/legacy_runs/A/`, `imogen/code/`, or via `find` on parent dirs.) | ~30 min |
| **2.3.3** | Read the IMOGEN engine state-machine sections in `imogen/code/imogen_lpjg.f` that consume SPINUP + FIRSTCALL inputs. Identify each branch + confirm whether hardcoded FALSE causes any branch divergence. | ~45 min |
| **2.3.4** | Re-examine `IMOGENConfig` namespace at `lpjguess/framework/parameters.h/cpp` to assess plumbing cost (how many LOC to add `FIRSTHISTYEAR` + `IYEND` parameters if needed). | ~15 min |
| **2.3.5** | Decision: (α) / (β) / (γ) per §2.1 acceptance. Document outcome in §2.5 landing record below. | ~15-30 min |
| **2.3.6** | If (α): commit only the landing record (doc-only) + the optional inline-comment tightening at `imogenoutput.cpp:342-350` if needed. If (β): commit the fix + test + landing record. If (γ): commit the landing record + the Phase 4 deferred sub-task addendum. | ~30 min |
| | **Phase 0 total** | **~2.5-3.0 h** |

### 2.4 Phase 0 commit anchor

Phase 0 closes with a commit on `b19-pipeline-verification` branch. Commit message anchor:

```
B19 Phase 0 (SPINUP/FIRSTCALL hardcoding investigation): <outcome α|β|γ>; landing record at notes/B19.md §2.5; <doc-only|N-LOC fix at imogenoutput.cpp + test|deferred to Phase 4>
```

### 2.5 Phase 0 landing record — outcome (β) HARDCODING IS WRONG; fix landed (this commit, 2026-05-16)

#### 2.5.1 Trigger + scope (per Q1=BEFORE locked in at session-5 opening)

**Trigger**: Phase 0 of the locked-in B19 work-plan per `notes/B19.md` §1.3 + §8 Q1=BEFORE decision. Phase 0 BEFORE Phase 4 was the locked-in ordering to pre-empt confounders. **Scope**: investigate whether SPINUP / FIRSTCALL hardcoded values at `lpjguess/modules/imogenoutput.cpp:357-359` are correct for v1.0 (single-process + MPI-multi-rank) modes, OR whether they represent a latent defect needing dynamic state-machine logic. **Performed**: 2026-05-16 morning by 6-step investigation per §2.3 (steps 2.3.1-2.3.6 all completed in single session).

#### 2.5.2 The 6 evidence anchors (smoking-gun synthesis)

| # | Anchor | What it shows |
|---|---|---|
| **E1** | `parameters.h:560-565` + `parameters.cpp:308-313` + `imogencfx.cpp:265-270` | All 6 control parameters (`YEAR1`, `IYEND`, `YEAR1_LPJG`, `SPINUP`, `KEEPRUNNING`, `FIRSTCALL`) ARE already declared + registered via `.ins` file. The inline comment at `imogenoutput.cpp:344-347` claiming "FIRSTHISTYEAR / IYEND ins parameters ... requires plumbing through the ins-file which is out of step-8 scope" is **STALE/FALSE** — the plumbing exists since step 8. |
| **E2** | `climatemodel.cpp:1181-1199` `updateImogenControlData()` | Dynamic state-machine ALREADY exists: `YEAR1++ / IYEND++` per engine iteration; `SPINUP` flips FALSE at `YEAR1==1901`; `FIRSTCALL` flips FALSE at `IYEND>1900`; `KEEPRUNNING` flips FALSE at `YEAR1==2100`. Called at `climatemodel.cpp:1172` inside the engine's IYEAR loop (per-iteration). |
| **E3** | `imogen_lpjg.f:447-448, 1865-1922` `SETTIN_LPJG` subroutine | Fortran engine reads `imogen_lpjg.txt` **each iteration of `DO WHILE (KEEPRUNNING)`** via `SETTIN_LPJG`, parsing SPINUP / KEEPRUNNING / FIRSTCALL each time. The LPJG-written file IS consumed every engine iteration — not just at startup. |
| **E4** | `imogen_lpjg.f:461-463, 482-484, 520-522, 876+, 1397` | SPINUP / FIRSTCALL are **substantively material** to engine behaviour: SPINUP gates `INCLUDE_CO2=.FALSE.` + `C_EMISSIONS=.FALSE.` (line 482-484) + `CO2_PPMV = CO2_INIT_PPMV` (line 520) + CO2.dat write logic (line 876); FIRSTCALL gates `VARYEAR=0` reset (line 461-463); KEEPRUNNING controls the outer `DO WHILE` loop (line 391+1397). |
| **E5** | Legacy A + Legacy B + checked-in scaffold `imogen_lpjg.txt` | All 3 reference files have `SPINUP TRUE / KEEPRUNNING TRUE / FIRSTCALL TRUE`. These are the bootstrap/seed values for the engine's very first iteration — exactly matching the `run_coupled.sh:317-326` bootstrap writer's values. |
| **E6** | `imogenoutput.cpp:31` already `#include "parameters.h"` | Fix is mechanically trivial — `IMOGENConfig::SPINUP / KEEPRUNNING / FIRSTCALL` are already accessible at the writer site; no additional plumbing needed. |

#### 2.5.3 Root cause

The `imogenoutput.cpp:357-359` hardcoding to `SPINUP FALSE / KEEPRUNNING TRUE / FIRSTCALL FALSE` was landed at step 8 commit `a543e9d` (2026-05-06) with an inline comment stating the plumbing was "out of step-8 scope". That comment was correct as a stand-alone rationale only briefly — steps 7 + 8 landed back-to-back, and step 8's own commit actually wired the full `IMOGENConfig` plumbing for all 6 parameters via `imogencfx.cpp:265-270 declare_parameter` calls. The writer was left with a defensive hardcoding that has been **factually obsolete since step 8's own close-out**, but the inline comment was never updated to reflect that. The defect is therefore a **stale-comment-driven hardcoding** that persisted unchallenged through 11 subsequent commits (step 9 → 17c.0.8) because no engine round-trip ever consumed a LPJG-written `imogen_lpjg.txt` in v1.0 (see §2.5.7 dormancy explanation).

#### 2.5.4 Decision (β) — APPROVED by user at session-5 opening (2026-05-16)

**Outcome (β) — HARDCODING IS WRONG; FIX REQUIRED.** Per `notes/B19.md` §2.1 acceptance criterion (β): "Fix by plumbing FIRSTHISTYEAR/IYEND through the imogen_intermediary.ins file" — except the plumbing was ALREADY done at step 8 (per E1+E6), so the fix is even simpler than the §2.1 acceptance anticipated. Per user AskQuestion approval 2026-05-16 morning ("I am leaning towards A, as long as you work meticulously, systematically, and comprehensively"). **Classification**: per the same AskQuestion (Q2), this closes **B19 sub-item (i)** without spawning a new B-letter audit item (since it's literally the sub-item we already opened — no need for the cumulative-naming convention to advance).

#### 2.5.5 Fix design (3 parts)

The fix is a single coherent edit at `lpjguess/modules/imogenoutput.cpp:341-401` (one `StrReplace`-tractable block):

**(F1) Inline-comment rewrite (~25 LOC, replaces lines 342-350)**. The 8-LOC stale `// SPINUP / KEEPRUNNING / FIRSTCALL flags...` block is replaced with a ~45-LOC explanatory block documenting:
- Forward dynamic-read pattern using `IMOGENConfig::*` state
- Cross-ref `climatemodel.cpp:1181-1199` `updateImogenControlData()` as the authoritative state machine
- Cross-ref `parameters.h:560-565` + `parameters.cpp:308-313` + `imogencfx.cpp:265-270` as the `.ins` plumbing source
- Cross-ref `imogen_lpjg.f:447-448, 1865-1922` `SETTIN_LPJG` as the engine consumer
- Cross-ref the 5 Fortran engine sites that substantively use SPINUP / FIRSTCALL (per E4)
- Dormancy explanation (F-10 deadlock dormancy + B19 surface)
- Forward-pointer to this landing record + `notes/B19.md §8 Q1=BEFORE` decision
- Grep-discovery anchor `[Step B19 Phase 0 (sub-item (i) closure) — 2026-05-16: ...]`

**(F2) New `dprintf` debug line (~5 LOC, inserted at line 388-392)**. Just before the `std::ofstream` open, emit forensic visibility of the SPINUP / KEEPRUNNING / FIRSTCALL values being written each year:
```cpp
dprintf("[ImogenOutput] flush_year(%d): next_year=%d SPINUP=%s KEEPRUNNING=%s FIRSTCALL=%s\n",
        year, next_year,
        IMOGENConfig::SPINUP      ? "TRUE" : "FALSE",
        IMOGENConfig::KEEPRUNNING ? "TRUE" : "FALSE",
        IMOGENConfig::FIRSTCALL   ? "TRUE" : "FALSE");
```
Cadence: per-year-once (102 lines for the smoke 4-cell × 2-year × 100-spinup run; acceptable noise). High-value forensic visibility for Phase 3 round-trip diagnostics — confirms the dynamic read is producing the expected pattern before the engine consumes it.

**(F3) Dynamic `IMOGENConfig::*` reads in the ofstream block (3 LOC pure substitution at lines 399-401)**. The 3 hardcoded FALSE/TRUE/FALSE lines become:
```cpp
f << "SPINUP "      << (IMOGENConfig::SPINUP      ? "TRUE" : "FALSE") << " !IN Are we in the spin-up phase of LPJ-GUESS?\n";
f << "KEEPRUNNING " << (IMOGENConfig::KEEPRUNNING ? "TRUE" : "FALSE") << " !IN control flag to keep imogen running\n";
f << "FIRSTCALL "   << (IMOGENConfig::FIRSTCALL   ? "TRUE" : "FALSE") << " !IN Is this the very first call to IMOGEN from LPJ-GUESS\n";
```

**Net change scale**: +54/-12 LOC in `imogenoutput.cpp` (per `git diff --numstat`). The substantive functional change is **3 LOC** (the ofstream-write substitution); the rest is comment + observability. `parameters.h` already included (no new `#include` needed per E6).

#### 2.5.6 Verification gates

| Gate | Description | Status |
|---|---|---|
| **V1** | Rebuild `lpjguess/build/` clean (no new warnings — pure substitution; `IMOGENConfig::*` already available via existing `#include "parameters.h"`) | _to be exercised after commit; expected: PASS_ |
| **V2** | 162/162 unit tests pass on rebuild (no regression — the change only affects file-write content; no unit test references `imogen_lpjg.txt`) | _to be exercised after commit; expected: PASS_ |
| **V3** | Spot-check: minimal `dprintf` shows SPINUP=TRUE pattern for the smoke 4-cell × 2-year run (years 1871-1872; YEAR1<1901 means SPINUP=TRUE expected for both bootstrap and flush-year). FULL verification requires Phase 3 actual round-trip; Phase 0 can only verify the dprintf compiles + fires. | _deferred to Phase 3_ |
| **V4** | No xval cross-validation regression: `scripts/cross_validate_year_outer.sh` + `scripts/cross_validate_mpi_4rank.sh` compare `.out` files which are unaffected by the `imogen_lpjg.txt` writer change. Confirmed safe by inspection. | _PASS by structural analysis; xval re-run optional but recommended at Phase 2 prep_ |

#### 2.5.7 Materiality + dormancy explanation

**Materiality** (per E4 engine-side analysis):

For the **smoke test (4-cell × 2-year, 1871-1872)**:

| Year | Source | Before fix | After fix (correct) | Material? |
|---|---|---|---|---|
| 1871 | bootstrap (`run_coupled.sh:317-326`) | SPINUP=TRUE, FIRSTCALL=TRUE | SPINUP=TRUE, FIRSTCALL=TRUE | ✅ already correct (bootstrap unchanged) |
| 1872 | LPJG `flush_year(1871)` writes for next year | SPINUP=FALSE, FIRSTCALL=FALSE | **SPINUP=TRUE** (YEAR1=1872<1901), FIRSTCALL=FALSE | ❌ → ✅ **NOW CORRECT** |

For the **production run (1871-2100)**:
- Years 1871-1900 (30 spinup years): was SPINUP=FALSE always → ❌ WRONG; now correctly SPINUP=TRUE per `IMOGENConfig::SPINUP` until YEAR1==1901
- Years 1901-2099: SPINUP=FALSE → ✅ unchanged (climatemodel's flip aligns; was coincidentally right)
- FIRSTCALL: always FALSE in flush_year → ✅ unchanged (bootstrap handles first iteration; matches new semantics)

**Dormancy** (per `notes/STEP_9.md` + `notes/FOLLOWUPS.md` F-10): In v1.0 single-process mode, the F-10 architectural deadlock means the engine never consumes a LPJG-written `imogen_lpjg.txt`. LPJG runs all years to completion before the engine gets back to its polling loop, so the engine's `SETTIN_LPJG` only ever reads the bootstrap file. The hardcoded-FALSE bug has been dormant since step 8 — it surfaces only once F-12 deadlock-resolution lands, which is exactly B19's surface. **Phase-0-BEFORE-Phase-4 ordering (per Q1=BEFORE decision) was the right call**: catching this defect now means Phase 4 closed-loop validation operates on canonical-correct behaviour rather than spending cycles attributing drift to a buggy SPINUP cascade.

#### 2.5.8 Per-file diff stats

| File | Insertions / Deletions | Substantive LOC | Comment / Observability LOC |
|---|---|---|---|
| `lpjguess/modules/imogenoutput.cpp` | +54 / -12 | 3 (the F3 ofstream substitution) | ~45 (F1 comment rewrite) + 5 (F2 dprintf) |
| `notes/B19.md` (this section §2.5) | ~+155 / -3 | 0 (doc-only) | ~155 (the landing record) |
| `notes/FOLLOWUPS.md` (B19 sub-item (i) closure annotation) | ~+3 / -1 | 0 | 3 |

Net at this commit: ~+212/-16 LOC. TRUNK-RELEVANCE: ALL TRUNK-IRRELEVANT-by-novelty (`imogenoutput.cpp` is novel to the rebuild — doesn't exist in `trunk_r13078`; same for `notes/B19.md`, `notes/FOLLOWUPS.md`).

#### 2.5.9 Stash entry retention

No stash this Phase 0 commit. Working tree clean post-commit; no pre-existing WIP exists.

#### 2.5.10 What's next (Phase 1 ramp)

Per `notes/B19.md` §1.3 + §3, the next phase is **B19 Phase 1: `intermediary_py` + step-13 adapter reproducibility re-run** (~45-90 min wall + ~30-60 min analysis). Phase 1 execution steps (per §3.2):
1. Save checked-in baseline outputs to scratch tmp (`/tmp/b19_phase1_baseline_*`)
2. Verify python env (`requirements.txt`)
3. Re-run `intermediary_py end-to-end` (`run_all.py --skip-plots`; ~9 min wall)
4. Re-run step-13 adapter on fresh output (~1-2 sec)
5. Reproducibility comparison via `diff -r` + `md5sum`
6. Decision per §3.1 acceptance (PASS / PARTIAL / FAIL)
7. Commit Phase 1 landing record + provide commit commands

No source-code change expected at Phase 1 (it's a reproducibility gate; pass/fail surfaces drift or confirms determinism). After Phase 1 lands, Phase 2 (engine state-machine prep + inputs verification; ~1-2 h) → Phase 3 (the scientific milestone: engine round-trip on workstation smoke 4-cell × 2-year; ~30-60 min wall + ~15-30 min analysis) → Phase 4 (closed-loop validation vs legacy A/B per Q2δ; ~2-6 h) → Phase 5 (close-out doc cascade + tag; ~2-3 h).

---

---

## 3. Phase 1 — `intermediary_py` + step-13 adapter reproducibility re-run

### 3.1 Scope + acceptance + outcomes

**Scope** (per Q3 = RETAIN FULL): re-execute the `intermediary_py` end-to-end pipeline (`run_all.py --skip-plots`) on the SSP1-2.6 default scenario + re-execute the step-13 adapter (`tools/imogen_inputs_to_lpjg_format.py`) on the fresh `intermediary_py` output. Compare both:

- **(R1)** new `intermediary_py/imogen_ghg_controller/outputs/imogen_inputs/imogen_inputs_SSP1-2.6.csv` vs checked-in counterpart (byte-equality via `diff`/`md5sum`)
- **(R2)** new `runs/SSP1-2.6/inputs/{co2_anthro_emissions.txt, ch4_n2o_anthro_emissions.txt, imogen_lpjg_flux.txt, imogen_lpjg_ch4_n2o_flux.txt}` vs checked-in counterparts (byte-equality)

**Acceptance criteria for Phase 1**:

- **PASS (R1 + R2)**: both reproducibility gates pass byte-exact. Close Phase 1 with landing record + zero code change.
- **PARTIAL (R1 fails, R2 fails)**: drift surfaces; characterise drift envelope; if drift is purely cosmetic (e.g. last-decimal FP-rounding, ordering-only, etc.), document + accept; if drift is substantive, treat as an audit item (B21+?) and decide go/no-go for Phase 3+4 with documented confounder.
- **PARTIAL (R1 passes, R2 fails)**: step-13 adapter drift; investigate adapter; should be deterministic so any drift is a real defect.
- **FAIL (both)**: substantive `intermediary_py` non-reproducibility; investigate dependency / env-state changes; decide remediation before Phase 3+4.

### 3.2 Phase 1 execution steps

| Step | Description | Anticipated time |
|---|---|---|
| **3.2.1** | Save checked-in baseline outputs to a scratch tmp dir: `cp -r intermediary_py/imogen_ghg_controller/outputs/imogen_inputs/ /tmp/b19_phase1_baseline_intermediary/`; `cp -r runs/SSP1-2.6/inputs/ /tmp/b19_phase1_baseline_adapter/`. | ~5 min |
| **3.2.2** | Stash any local changes (none expected on clean branch); switch to `intermediary_py/imogen_ghg_controller/`; verify python env (`requirements.txt` → `python3 -m pip install -r requirements.txt --user` if needed). | ~5-15 min |
| **3.2.3** | Re-run `intermediary_py` end-to-end: `cd intermediary_py/imogen_ghg_controller; python3 run_all.py --skip-plots`. Expected wall-clock ~9 min per `intermediary_py/README.md`. | ~10-15 min (wall) |
| **3.2.4** | Re-run step-13 adapter on fresh output: `cd ../..; python3 tools/imogen_inputs_to_lpjg_format.py --input intermediary_py/imogen_ghg_controller/outputs/imogen_inputs/imogen_inputs_SSP1-2.6.csv --output runs/SSP1-2.6/inputs/`. Expected wall-clock ~1-2 sec. | ~2 min |
| **3.2.5** | Reproducibility comparison: `diff -r /tmp/b19_phase1_baseline_intermediary/ intermediary_py/imogen_ghg_controller/outputs/imogen_inputs/` + `diff -r /tmp/b19_phase1_baseline_adapter/ runs/SSP1-2.6/inputs/` + cross-check via `md5sum`. | ~10 min |
| **3.2.6** | If diffs surface, characterise envelope: `git diff --stat` + `head`/`tail` of differing files + classify (cosmetic / FP-rounding / substantive). | ~15-45 min depending on outcome |
| **3.2.7** | Decision per §3.1 acceptance + commit landing record. If PASS, restore the checked-in baseline (any new files re-checked-in should be byte-identical and revert cleanly). | ~15 min |
| | **Phase 1 total** | **~45-90 min wall + ~30-60 min analysis** |

### 3.3 Phase 1 commit anchor

```
B19 Phase 1 (intermediary_py + step-13 adapter reproducibility re-run): <PASS|PARTIAL|FAIL>; <details>; landing record at notes/B19.md §3.4
```

### 3.4 Phase 1 landing record (to be filled in at Phase 1 close)

_TBD — Phase 1 not yet executed at this commit (notes/B19.md initial commit)._

---

## 4. Phase 2 — IMOGEN engine state-machine prep + inputs verification

### 4.1 Scope + acceptance + outcomes

**Scope**: Verify that the rebuild's IMOGEN engine has all inputs it needs to execute a full round-trip in `coupling_mode = prescribed` on the SSP1-2.6 smoke configuration (4-cell × 2-year, 1871-1872). This is the staging/integration phase that lines up Phase 3.

**Acceptance criteria for Phase 2**:

- **(A1)** `lpjguess/build/guess` exists + is recently rebuilt (mtime within last week, or rebuild if not).
- **(A2)** `runs/SSP1-2.6/main.ins` + `runs/SSP1-2.6/imogen_intermediary.ins` reference the correct adapter outputs (intermediary-py backbone path is wired correctly per `notes/STEP_13.md` §1).
- **(A3)** `runs/SSP1-2.6/Common-directory/LPJG_main/IMOGEN/` exists + has bootstrap `imogen_lpjg.txt` + `done` per `run_coupled.sh:317-326`.
- **(A4)** Adapter outputs (from Phase 1) are in place at `runs/SSP1-2.6/inputs/` (4 ASCII files).
- **(A5)** Engine binary `imogen/code/imogen_lpjg.x` (or equivalent) is built + executable; Fortran build deps satisfied.
- **(A6)** Phase 0 outcome is reflected in `imogenoutput.cpp` (if (β) was chosen) or in this doc (if (α) or (γ)).

If any A* fails, fix it in Phase 2 + commit the fix + landing record.

### 4.2 Phase 2 execution steps

| Step | Description | Anticipated time |
|---|---|---|
| **4.2.1** | Verify A1: `ls -la lpjguess/build/guess` + `make -C lpjguess/build` (rebuild if stale). | ~5-10 min |
| **4.2.2** | Verify A2: read `runs/SSP1-2.6/imogen_intermediary.ins` Option-B block to confirm adapter outputs are referenced correctly. | ~5 min |
| **4.2.3** | Verify A3: `ls -la runs/SSP1-2.6/Common-directory/LPJG_main/IMOGEN/`; if missing, bootstrap via `run_coupled.sh --no-build --no-intermediary --no-adapter` partial invocation (or manual `mkdir -p` + cat-out the bootstrap files). | ~5-10 min |
| **4.2.4** | Verify A4: `ls -la runs/SSP1-2.6/inputs/` + spot-check content (head of each file). | ~5 min |
| **4.2.5** | Verify A5: check IMOGEN engine binary path + build status; if not built, build via `make -C imogen/code/`. | ~10-30 min depending on build state |
| **4.2.6** | Confirm A6 reflects Phase 0 outcome correctly. | ~5 min |
| **4.2.7** | Commit Phase 2 landing record (doc-only if all A* pass without modification; doc + small fixes if anything had to be rebuilt or rewired). | ~15 min |
| | **Phase 2 total** | **~1-2 h** |

### 4.3 Phase 2 commit anchor

```
B19 Phase 2 (IMOGEN engine state-machine prep + inputs verification): all A1-A6 PASS; <any fixes>; landing record at notes/B19.md §4.4
```

### 4.4 Phase 2 landing record (to be filled in at Phase 2 close)

_TBD — Phase 2 not yet executed at this commit (notes/B19.md initial commit)._

---

## 5. Phase 3 — IMOGEN engine round-trip on workstation (smoke 4-cell, 2-year)

### 5.1 Scope + acceptance + outcomes

**Scope** (the actual scientific milestone): execute `scripts/run_coupled.sh --backbone intermediary-py --coupling-mode prescribed --scenario SSP1-2.6 --smoke` end-to-end on the workstation. Confirm the IMOGEN Fortran engine reads the LPJG handshake files + adapter anthro emissions correctly, runs the climate emulator forward, and writes `CO2.dat` + climate-anomaly fields to `runs/SSP1-2.6/Common-directory/IMOGEN/output/<year>/`.

**Acceptance criteria for Phase 3**:

- **(B1)** `run_coupled.sh` completes WITHOUT F-10 deadlock (the v1.0-known F-10 deadlock is the original deferred-future-sub-phase concern; Phase 3 should not hit it on the smoke 4-cell × 2-year config in prescribed mode per `scripts/run_coupled.sh:340-350` expectations).
- **(B2)** LPJG handshake files written: `Common-directory/LPJG_main/IMOGEN/imogen_lpjg_flux.txt` + `imogen_lpjg_ch4_n2o_flux.txt` exist with **2+ lines** (year 1871 + year 1872; per `run_coupled.sh:362-372` summary check).
- **(B3)** Engine output exists: `Common-directory/IMOGEN/output/<year>/` directories created with `CO2.dat` and other engine-output files; year coverage matches the smoke config.
- **(B4)** `CO2.dat` has **8+ columns** (year + CO2_total + CO2_anthro + CO2_natural + atmospheric concentration + ... + CH4 concentration as col 7 + N2O concentration as col 8 per Fortran engine emission convention).
- **(B5)** No NaN or Inf values in engine outputs (sanity check via `awk` / `grep`).
- **(B6)** No undocumented warnings/errors in the engine log (Fortran stderr captured via `tee -a "${LOG_FILE}"`).

If any B* fails, investigate + decide whether to fix-in-place in Phase 3 or escalate as a B19-blocker.

### 5.2 Phase 3 execution steps

| Step | Description | Anticipated time |
|---|---|---|
| **5.2.1** | Clean up any stale per-year IMOGEN output: `rm -rf runs/SSP1-2.6/Common-directory/IMOGEN/output/` (per `run_coupled.sh:336` pattern). | ~1 min |
| **5.2.2** | Invoke `scripts/run_coupled.sh --backbone intermediary-py --coupling-mode prescribed --scenario SSP1-2.6 --smoke` end-to-end. Capture full log to `logs/`. | ~5-15 min (wall, depending on engine speed) |
| **5.2.3** | B1 verification: confirm `run_coupled.sh` exited 0 (or with the documented F-10-related EC if it occurred). | ~1 min |
| **5.2.4** | B2 verification: `wc -l runs/SSP1-2.6/Common-directory/LPJG_main/IMOGEN/imogen_lpjg_{flux,ch4_n2o_flux}.txt`. | ~1 min |
| **5.2.5** | B3 verification: `ls -la runs/SSP1-2.6/Common-directory/IMOGEN/output/`. | ~1 min |
| **5.2.6** | B4 verification: `head -3 runs/SSP1-2.6/Common-directory/IMOGEN/output/<year>/CO2.dat`; confirm 8+ columns. | ~2 min |
| **5.2.7** | B5 verification: `grep -E 'nan\|NaN\|inf\|Inf' runs/SSP1-2.6/Common-directory/IMOGEN/output/<year>/*.dat`. | ~2 min |
| **5.2.8** | B6 verification: scan log for unexpected warnings/errors. | ~5 min |
| **5.2.9** | Commit Phase 3 landing record + (optionally) the run log artefact under `_chat_artifacts/b19_phase3_smoke_run/`. | ~15 min |
| | **Phase 3 total** | **~30-60 min wall + ~15-30 min analysis** |

### 5.3 Phase 3 commit anchor

```
B19 Phase 3 (IMOGEN engine round-trip workstation smoke 4-cell 2-year): all B1-B6 PASS; engine round-trip end-to-end CO2.dat with CH4+N2O cols 7+8 produced; landing record at notes/B19.md §5.4
```

### 5.4 Phase 3 landing record (to be filled in at Phase 3 close)

_TBD — Phase 3 not yet executed at this commit (notes/B19.md initial commit)._

---

## 6. Phase 4 — Closed-loop validation vs legacy A/B (defer-to-empirics tolerance)

### 6.1 Scope + acceptance + outcomes

**Scope** (per Q2 = DEFER-TO-EMPIRICS WITH MIXED FALLBACK): cross-validate the Phase 3 engine output (CO2.dat + climate-anomaly fields) against legacy A reference values for the equivalent smoke configuration (SSP1-2.6, 4-cell, 1871-1872). The validation is **the actual scientific PASS criterion for B19**. Phase 4 runs in two sub-stages:

- **Stage 4a — no-gate empirical characterisation**: compute relative drift vs legacy A for every comparable column (CO2 total, CH4 conc, N2O conc, climate-anomaly fields if available). Report drift envelope per column + per field type.
- **Stage 4b — tolerance lock-in**: based on observed envelope, select tolerance:
  - If drift universally ≤1% on GHG concs → lock in **strict** (≤1%) on GHG; ≤5% on intermediate climate fields if applicable.
  - If drift is uneven across field types → lock in **mixed** (strict on GHG concs; loose on intermediate climate fields per the fallback policy).
  - If drift is universally larger but explainable (e.g. F-10 deadlock workaround introduces a known year-shift) → document the explanation + accept with relaxed tolerance.

**Acceptance criteria for Phase 4**:

- **(C1)** Stage 4a completes: drift envelope characterised per column + per field type.
- **(C2)** Stage 4b completes: tolerance locked in + PASS gate applied.
- **(C3)** Closed-loop validation reports PASS per the locked-in tolerance.

### 6.2 Phase 4 execution steps

| Step | Description | Anticipated time |
|---|---|---|
| **6.2.1** | Locate legacy A's reference engine output for the equivalent smoke config (likely under `_chat_artifacts/legacy_runs/A/` or analogous; if not present, request from user or document as a B19-blocker). | ~15-30 min |
| **6.2.2** | Write a small `python3` comparison script at `scripts/b19_phase4_closed_loop_validate.py` (or shell `awk` equivalent) computing relative drift per column. Save to `_chat_artifacts/b19_phase4_drift/`. | ~30-60 min |
| **6.2.3** | Run Stage 4a (no-gate report) + capture drift envelope per column. | ~10-15 min |
| **6.2.4** | Stage 4b decision (per §6.1 logic) + apply gate. | ~15-30 min |
| **6.2.5** | If C3 = PASS: commit Phase 4 landing record + the comparison script + drift artefacts. If C3 = FAIL: investigate (re-check Phase 0/1/2/3 outcomes; bisect drift sources; decide go/no-go for B19 closure or escalate as a B19-blocker). | ~30 min - several hours depending on outcome |
| | **Phase 4 total** | **~2-6 h** depending on outcome |

### 6.3 Phase 4 commit anchor

```
B19 Phase 4 (closed-loop validation vs legacy A/B with defer-to-empirics tolerance): C1+C2+C3 PASS at tolerance <strict|mixed|relaxed>; drift envelope <range>; landing record at notes/B19.md §6.4
```

### 6.4 Phase 4 landing record (to be filled in at Phase 4 close)

_TBD — Phase 4 not yet executed at this commit (notes/B19.md initial commit)._

---

## 7. Phase 5 — Documentation cascade + B19 close-out tag

### 7.1 Scope + acceptance + outcomes

**Scope**: 6-surface in-tree doc cascade + 1 sibling-artifact + git workflow (merge to main + annotated tag + 3-remote push) to formally close B19. Analogous to 17c.0.8 PREP close-out (per `notes/STEP_17c.md` §1.7).

**Per-surface scope**:

| # | Surface | Type | Expected delta |
|---|---|---|---|
| 1 | `notes/B19.md` (this file) | UPDATE | each phase's §X.Y landing record filled in; §0 status promoted to ✅ CLOSED; §1.3 phase table updated with actual outcomes; §8 risk register closed-out |
| 2 | `notes/FOLLOWUPS.md` | UPDATE | B19 entry status changed from PROMOTED to NEXT-TASK CLUSTER → ✅ CLOSED at this commit; B20 entry advanced to PRIORITY: HIGH + NEXT IN QUEUE |
| 3 | `CHANGELOG.md` | INSERT | NEW dated entry under [Unreleased] for B19 close-out |
| 4 | `EXECUTION_PLAN.md` | UPDATE | row 18 (closed-loop validation) and/or new row B19 marked ✅ DONE |
| 5 | `notes/TRUNK_R13078_BACKPORT_LEDGER.md` | INSERT | NEW B19 entry at end of §3 (likely TRUNK-IRRELEVANT-by-novelty in entirety unless Phase 0 outcome (β) landed substantive trunk-relevant changes) |
| 6 | `notes/STEP_17c.md` | UPDATE | §1.7.8 "What's next" updated to reflect B19 CLOSED + B20 ACTIVE |
| _sibling_ | `_chat_artifacts/CHAT_HANDOFF_2026-05-12_session3.md` (or new session 5+ handoff file) | INSERT or NEW | NEW Part 9 (or new file) — B19 close-out narrative + handoff to B20 |

**Tag**: `v0.19.0-b19-closed-loop-verified` (or, if Phase 4 outcome warranted a partial-PASS designation, a more precise variant).

**Acceptance**: all 6 surfaces updated; tag pushed to all 3 remotes (origin + helmholtz + kit); 3-remote convergence verified.

### 7.2 Phase 5 execution steps

| Step | Description | Anticipated time |
|---|---|---|
| **7.2.1** | Update notes/B19.md §0 + §1.3 + all §X.4/§X.5 landing records (this file). | ~30 min |
| **7.2.2** | Update notes/FOLLOWUPS.md B19+B20 entries + status dashboard. | ~15 min |
| **7.2.3** | Insert CHANGELOG.md entry. | ~10 min |
| **7.2.4** | Update EXECUTION_PLAN.md row 18 + (potentially) new row B19. | ~10 min |
| **7.2.5** | Insert notes/TRUNK_R13078_BACKPORT_LEDGER.md B19 entry. | ~10 min |
| **7.2.6** | Update notes/STEP_17c.md §1.7.8. | ~5 min |
| **7.2.7** | Append CHAT_HANDOFF Part 9 (or new session 5+ handoff file). | ~30 min |
| **7.2.8** | Verify all 6+1 surfaces via git diff --stat + ReadLints. | ~10 min |
| **7.2.9** | Commit + checkout main + fast-forward merge + annotated tag + push to 3 remotes. | ~10-15 min |
| **7.2.10** | Sanity verify 3-remote convergence + tag visibility. | ~5 min |
| | **Phase 5 total** | **~2-3 h** |

### 7.3 Phase 5 commit anchor

```
B19 Phase 5 (close-out doc cascade + tag v0.19.0-b19-closed-loop-verified): 6-surface in-tree cascade + 1 sibling-artifact; all phases ✅ CLOSED; B20 promoted to ACTIVE NEXT; landing record at notes/B19.md §7.4
```

### 7.4 Phase 5 landing record (to be filled in at Phase 5 close)

_TBD — Phase 5 not yet executed at this commit (notes/B19.md initial commit)._

---

## 8. Locked-in design decisions (Q1/Q2/Q3 from session-5 opening)

| # | Question | Decision | Rationale | Reflected in |
|---|---|---|---|---|
| **Q1** | SPINUP/FIRSTCALL ordering vs Phase 4 | **(a) BEFORE** Phase 4 | Investigation is cheap (~0.5 d); pre-empts confounders; cleanest experimental discipline | §1.3 phase ordering + §2 Phase 0 |
| **Q2** | Phase 4 closed-loop validation tolerance | **(δ) DEFER-TO-EMPIRICS with mixed-tolerance fallback** | No prior data on 8-stage pipeline drift envelope; forensically valuable to characterise first then lock in; mixed fallback handles uneven drift across field types | §6.1 Phase 4 two-stage structure |
| **Q3** | Phase 1 ordering revisit | **(i) RETAIN FULL** | Re-running `intermediary_py` end-to-end is cheap (~30-60 min wall); reproducibility gate after the long gap since step-11; early signal for env/dep changes | §1.3 Phase 1 retained at full scope; §3 Phase 1 |

Each decision is encoded structurally in the work-plan (not just in this section). Future doc-cascade edits should cite this section + the encoded structure rather than re-deriving.

---

## 9. Risk register + open questions

| # | Risk / Open Q | Severity | Mitigation / Resolution path |
|---|---|---|---|
| **R1** | Phase 0 outcome (γ) inconclusive → cascades into Phase 4 deferred sub-task → Phase 4 PASS may need re-investigation | LOW | Phase 0 step 2.3.3 (engine state-machine trace) is the highest-information step; if it's done meticulously, (γ) should be avoidable |
| **R2** | Phase 1 reproducibility drift (R1 or R2 fails) → potential B21 audit item | LOW-MEDIUM | `intermediary_py` is deterministic by design; drift would indicate env/dep changes; characterise envelope + decide go/no-go per §3.1 |
| **R3** | Phase 3 hits F-10 deadlock on smoke 4-cell × 2-year (per `run_coupled.sh:340-350` expectations this should not happen on smoke in prescribed mode, but it's a known v1.0 risk) | LOW | If deadlock occurs, characterise + decide whether to fix-in-place or document as a Phase 3 blocker; F-12 architectural resolution is the eventual fix |
| **R4** | Phase 4 legacy A reference data not available locally → cannot complete closed-loop validation | MEDIUM | Phase 4 step 6.2.1 surfaces this early; if missing, request from user / re-construct from version_A predecessor data |
| **R5** | Phase 4 drift larger than expected → re-investigation cycle through Phase 0/1/2/3 | MEDIUM | Phase 0 BEFORE ordering (Q1=a) is the principal mitigation; Phase 1 reproducibility gate + Phase 2 inputs verification are additional layers |
| **R6** | B19 wall-clock exceeds 4 d estimate → 17c.1+ cluster phases push out | LOW | Estimate has buffer (2.5-4 d range); if exceeded, document + re-estimate at B19 close-out per the §1.7.6 forecast-vs-actual retrospective pattern |
| **OQ1** | Should the harness for `run_coupled.sh` smoke verification be checked in as a script at `scripts/cross_validate_b19_smoke.sh` analogous to `scripts/cross_validate_mpi_4rank.sh`? | OPEN | Defer to Phase 3/5; if Phase 3 surfaces a need for harness-based regression testing, add as Phase 5 sub-task |
| **OQ2** | Should the closed-loop comparison script at scripts/b19_phase4_closed_loop_validate.py be designed for reuse in 17c.1+ cluster phases (production gridlist) and B20 (literature comparison)? | OPEN | Defer to Phase 4 design; if structured well it could serve all three; otherwise scope it for B19 only and re-write for B20+17c.1 |

---

## 10. References

### 10.1 In-tree primary sources

- `lpjguess/modules/imogenoutput.cpp` — `ImogenOutput::flush_year` and the SPINUP/FIRSTCALL hardcoding (lines 263-378; specifically 336-364)
- `lpjguess/modules/imogencfx.cpp` — IMOGEN engine polling loop + F-4 fix surface
- `lpjguess/framework/parameters.h/cpp` — `IMOGENConfig` namespace (target for any Phase 0 (β) plumbing fix)
- `imogen/code/imogen_lpjg.f` — IMOGEN Fortran engine state-machine + Fortran reader format definitions
- `tools/imogen_inputs_to_lpjg_format.py` — step-13 adapter (unit-checked-adapter discipline)
- `scripts/run_coupled.sh` — workstation launcher (7-stage orchestration)
- `intermediary_py/imogen_ghg_controller/run_all.py` — `intermediary_py` end-to-end driver
- `intermediary_py/imogen_ghg_controller/outputs/imogen_inputs/imogen_inputs_SSP1-2.6.csv` — checked-in baseline for Phase 1 (R1) gate
- `runs/SSP1-2.6/inputs/{co2,ch4_n2o}_anthro_emissions.txt + imogen_lpjg{,_ch4_n2o}_flux.txt` — checked-in baseline for Phase 1 (R2) gate
- `runs/SSP1-2.6/imogen_intermediary.ins` — engine .ins with Option-A (static-IIASA) + Option-B (intermediary-py adapter) + Option-C (relative-path tight-mode) blocks

### 10.2 In-tree doc surfaces

- `EXECUTION_PLAN.md` — V.1 steps 8/13/14/17/18/19 + Decisions #1/#8/#11
- `notes/STEP_7.md` — F-4 fix at IMOGEN-side polling loop (relevant to Phase 0 step 2.3.1)
- `notes/STEP_8.md` — LPJG-side handshake writer (origin of SPINUP/FIRSTCALL hardcoding at commit `a543e9d`)
- `notes/STEP_9.md` — F-10 deadlock empirical findings (relevant to Phase 3 R3 risk mitigation)
- `notes/STEP_11.md` — `intermediary_py` end-to-end run details (relevant to Phase 1)
- `notes/STEP_13.md` — step-13 adapter (relevant to Phase 1)
- `notes/STEP_14.md` — `run_coupled.sh` design (relevant to Phase 3)
- `notes/STEP_17c.md` — 17c.0 PREP arc + §1.7.8 session-5 opening agenda
- `notes/FOLLOWUPS.md` — B19 + B20 entries + F-10 + F-12 architectural items
- `notes/TRUNK_R13078_BACKPORT_LEDGER.md` — backport classification ledger

### 10.3 Sibling-artifact references

- `_chat_artifacts/CHAT_HANDOFF_2026-05-12_session3.md` Part 8 — 17c.0.8 PREP close-out narrative + 3 deferred B19 questions (Q1/Q2/Q3)
- _new_: `_chat_artifacts/CHAT_HANDOFF_<date>_session5*.md` — session 5 narrative (B19 phases as they land)

### 10.4 External / scientific references

- Tian et al. 2020 GCB — global N2O budget (relevant to B20 literature comparison, post-B19)
- Saikawa et al. 2014 ACP — historical N2O attribution (relevant to B20)
- IPCC AR6 WG1 Ch. 5 — N2O concentration trajectories (relevant to B20)
- Saunois et al. 2020 ESSD — global methane budget (relevant to B20 CH4 comparison)
- IPCC 2019 Refinement Tier 1 — `intermediary_py` Component A methodology

---

## 11. Index of landing records (live-updated)

| Phase | Landing record location | Status |
|---|---|---|
| 0 | §2.5 | ✅ **DONE at this commit (2026-05-16)** — outcome (β) hardcoding fix landed; B19 sub-item (i) ✅ CLOSED |
| 1 | §3.4 | _PENDING — Phase 1 NEXT_ |
| 2 | §4.4 | _PENDING_ |
| 3 | §5.4 | _PENDING_ |
| 4 | §6.4 | _PENDING_ |
| 5 | §7.4 | _PENDING (B19 close-out)_ |

---

_End of `notes/B19.md` — live-updated as phases land. Phase 0 landed at this commit (2026-05-16); 5 phases remaining (1 + 2 + 3 + 4 + 5)._
