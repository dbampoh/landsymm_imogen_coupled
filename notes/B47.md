# B47 — Option T_seq: trunk_r13078 minimal-backport for sequential-standalone Track 2 paper-publication runs (session 8.0 C5 strategic decision record)

**Status**: ✅ DONE (decision-recorded) — session 8.0 C5 strategic decision recorded at commit `d6ef6c1` (2026-05-20 afternoon, session 8 opening). Subsequent implementation work tracked separately:
- **Block 8.0.1 ✅ DONE 2026-05-20 afternoon session 8.0.1 close** — structural import of `trunk_r13078` LPJG fork into rebuild repo at `forks/trunk_r13078/` (sibling to `lpjguess/`); ZERO source-edit at import (verbatim upstream content; 23 MB / 341 files via rsync with build/dev exclusions); NEW `forks/README.md` (~115 LOC) two-fork policy + dual-fork long-term trajectory + build instructions; baseline build verification ✅ PASS with NEW B48 `-DCMAKE_EXE_LINKER_FLAGS="-lcurl"` system-packaging workaround documented (affects BOTH `lpjguess/` and `forks/trunk_r13078/` equally on Ubuntu 24.04+ with `libhdf5-310` 1.14.5+; rebuild's pre-built May-16 binaries still work because linked before system update); 8-gate verification V0-V7 all PASS; Rule #9 datapoint #15 + Rule #10 at 15th consecutive datapoint. Full landing record at `notes/TRUNK_R13078_BACKPORT_LEDGER.md` §3 "Block 8.0.1 LANDED" entry.
- **Block 8.0.2 ✅ DONE 2026-05-20 late afternoon session 8.0.2 close** — T_seq Installment-1 source-edit applied (~251 LOC C++ across 4 files in `forks/trunk_r13078/` — imogencfx.cpp +208 / imogencfx.h +29 / parameters.cpp +11 / parameters.h +15); all 6 surgical sites per §4.2 applied; per-year CO2.dat reader (g) needed ZERO LOC (trunk's existing pattern already supports YYYY-templated reads); NEW trunk-Track-2 `.ins` set authored at `forks/trunk_r13078_runs/SSP1-2.6/` (19 files: NEW README.md + NEW main.ins for T_seq + modified imogen_intermediary.ins [Windows→Linux + skip_inprocess_engine_run 1 + B39 296.1/875.6/277.4 Law Dome 1900 seeds] + 16 cp'd predecessor production-grade .ins from `lpjg_landsymm_integration/integrated-4.1-ins2_landsymm_hist/`); build verification ✅ PASS post-edit producing `guess` 2,920,856 bytes (+7,968 vs pre-edit; expected B4 wiring delta); V0-V7 verification all PASS; Rule #10 at 16th consecutive datapoint. Full landing record at `notes/TRUNK_R13078_BACKPORT_LEDGER.md` §3 "Block 8.0.2 LANDED" entry.
- **Block 8.0.3 ⏳ ACTIVE NEXT** — local 4-cell smoke acceptance test 8-gate evaluation per §4.3: pre-run setup symlink `ln -s ../../../runs/SSP1-2.6/Common-directory ./Common-directory` (engine library from B44 acceptance test 2026-05-19) + override main.ins gridlist to `../../../data/gridlist/gridlist_test2.txt` (rebuild's 4-cell smoke matching engine library) + invoke `forks/trunk_r13078/build/guess -input imogencfx main.ins`; audit-evidence bundle at `_chat_artifacts/b47_tseq_acceptance_4cell_2026-05-20/`. After ✅ PASS: tag candidate `v0.22.0-tseq-installment-1-complete` + 3-remote push.

**Priority**: ⚠️ HIGH (paper-publication critical-path; sequenced before block 8.1 cluster reconnaissance per session 8 plan §1.2 since 8.1-8.7 retarget to `forks/trunk_r13078/` post-T_seq).

**Estimated effort**: ~2-3 working days cumulative (structural import ~0.5 d + source backport ~0.5-1 d + .ins authoring ~0.5 d + acceptance test ~0.5-1 d + cascade ~0.5 d).
**Actual effort**: TBD at block 8.0.3 close.

**Trigger**: user-raised strategic question at session 7 close (2026-05-19 12:53 AM per `notes/CLUSTER_SETUP_AND_PRODUCTION_RUNS.md` §0.2 + `notes/TRUNK_R13078_BACKPORT_LEDGER.md` strategic note) about Track 2 LPJG version choice for paper-reviewer-defensibility. C0-C5 verification + U1-U4 pre-flight reconnaissance executed at session 8.0 opening (this session); outcome: Option T_seq adopted (sequential-standalone workflow with minimal trunk_r13078 backport).

**Cross-references**:
- `notes/CLUSTER_SETUP_AND_PRODUCTION_RUNS.md` §0.2 (the strategic question framing + C0-C5 checklist; this record provides the C5 evidence + decision outcome)
- `notes/TRUNK_R13078_BACKPORT_LEDGER.md` strategic note at top (direction-inversion confirmation + revised cumulative-backport-debt accounting under T_seq)
- `notes/PAPER_COMPLETION_AND_VALIDATION.md` §1.4 (Axis 4 validation interpretation; T_seq resolves the LPJG-version-confounding concern)
- `notes/B44.md` (rebuild's `--engine-only-mode` flag; the productisation T_seq depends on for the engine-side standalone run)
- `notes/B37.md` (path-iv `done`-marker sidecar mechanism that B44 productised; engine runs to completion via this on 1900-2100 = 201 year-dirs + 1 over-shoot placeholder)
- `notes/FOLLOWUPS.md` B19/B36/B39/B40 entries (audit items already validated against rebuild; T_seq inherits their validation via the engine-side standalone-run + trunk's LPJG-side N/A status)
- `lpj-guess_imogen_landsymm/scripts/run_coupled.sh` (rebuild launcher with `--engine-only-mode`; produces the per-year ASCII library T_seq's trunk-LPJG consumes)
- `version_A/LPJG-IMOGEN-COUPLED-MODEL-FRAMEWORK/Integrations/trunk/trunk_r13078/modules/` (the trunk_r13078 source to be imported as `forks/trunk_r13078/` at block 8.0.1)
- `lpjg_landsymm_integration/integrated-4.1-ins2_landsymm_{hist,ssp126}/` (existing production-style `.ins` files for `-input cfx` Track 1 mode; adapted to trunk-Track-2 `.ins` set at block 8.0.2)

---

## 0. Headline finding (1-paragraph TL;DR for future maintainers + paper authors)

✅ **Option T_seq selected as session 8.0 C5 decision**: trunk_r13078 (the LPJG version that produced Track 1's natural-flux outputs) is brought into the rebuild repo as `forks/trunk_r13078/` (sibling to `lpjguess/`), receives a minimal source-edit (~180-220 LOC minimum-viable; ~280-380 LOC including symmetric `-input imogen` consumer wiring) to consume the rebuild engine's per-year ASCII climate library, and runs Track 2 production runs as **standalone LPJG-only** (no in-process engine) reading the pre-baked engine-output library. The rebuild's IMOGEN engine continues to run standalone first per SSP via `scripts/run_coupled.sh --engine-only-mode` (B44 productised), producing the full 1900-2100 × 8-field climate + per-year CO2 + per-year non-CO2 atmospheric concentrations library at `runs/<SCEN>/Common-directory/IMOGEN/output/<year>/`.

**Two-fork long-term trajectory (user-clarified at session 8.0 mid-discussion 2026-05-20)**:

- **Primary fork — `lpjguess/`** (the rebuild's LPJ-GUESS): the **active development surface** for all v1.0+ work going forward (B45 hardcoded year sentinels; B46 N2O channel split; F-10/F-12 architectural deadlock resolution + v1.1+ tight coupling; v1.5+ ecosystem refinements; v2.0+ PLUM embedding). All future scientific + architectural work lands here first. Already exercises 8-field climate + per-year atmospheric concentrations + step-8 `imogenoutput.cpp` LPJG-side handshake-writer + step-17a year_outer scaffolding + step-17b MPI sync + all B-series engine-side fixes. This is the canonical rebuild surface.
- **Backport fork — `forks/trunk_r13078/`** (post-block-8.0.1): the **paper-publication Track 2 runs surface for v1.0** + the **long-term backport target** to be **eventually brought fully up to speed with `lpjguess/` so both forks are switchable alternatives** (not one replacing the other). The T_seq minimal-backport at block 8.0.2 (~180-310 LOC) is **the first installment** of the eventual full-fork-parity Backport Sprint per `notes/TRUNK_R13078_BACKPORT_LEDGER.md` §1.1-§1.2 — it gets `forks/trunk_r13078/` to a Track-2-runnable state with the bare-minimum source-edits needed to consume rebuild-engine outputs in T_seq sequential-standalone workflow. The remaining ~2900-3100 LOC of full-fork-parity backport (year_outer scaffolding + `imogenoutput.cpp` + `climatemodel.cpp` engine-side + Fortran step-3/B10/B33(c)) stays scheduled for the post-paper Backport Sprint as originally framed in ledger §1.2, completing the parity work so both forks can serve as switchable reference implementations.

This dual-fork lifestyle aligns with `notes/TRUNK_R13078_BACKPORT_LEDGER.md` §1.1's "both forks then live in the rebuild as reference implementations" policy + user's clarification that **trunk_r13078 is the backport fork** intended **as an alternative, not a replacement**. The session-7-close preliminary "~150-300 LOC; few-hour to ~1-day" T-estimate was over-optimistic by ~3-5× when interpreted as in-process T1/T2 (~1300-1600 LOC); honest re-baselining at C0-C4 surfaced this Rule-#10 self-correction; user's session-8 refinement to sequential-standalone workflow (T_seq) brought the **first-installment** scope back down to ~280-380 LOC by dropping year_outer scaffolding (~400 LOC), `imogenoutput.cpp` rewrites (~821 LOC), `climatemodel.cpp` engine-side delta (~263 LOC), and Fortran backport-debt (~562 LOC total) — these stay deferred to the post-paper full-parity Backport Sprint. **Total estimated calendar effort for the T_seq first-installment: ~2-3 working days across blocks 8.0.1-8.0.3.**

---

## 1. Background — the strategic question + the four-option enumeration

Per `notes/CLUSTER_SETUP_AND_PRODUCTION_RUNS.md` §0.2 (session 7 close framing):

| Option | Use this LPJG for Track 2 | Backport effort (session-7-close preliminary) | Backport effort (post-C0-C4 honest) | Paper integrity |
|---|---|---|---|---|
| **R** (rebuild; current default) | `lpj-guess_imogen_landsymm/` | 0 LOC | 0 LOC | LPJG-version-confounded between Track 1 (trunk_r13078) and Track 2 (rebuild later-4.1-base) |
| **T1** (in-process B44 sidecar into trunk_r13078) | `trunk_r13078/` | ~150-300 LOC | ~1300-1600 LOC (per-rank sidecar; year_outer scaffolding; imogenoutput; climatemodel delta; Fortran) | LPJG-version held constant |
| **T2** (decoupled engine + skip-engine flag) | `trunk_r13078/` | ~150-300 LOC | ~1350-1650 LOC (skip-engine flag; same C++/Fortran scope as T1 minus per-rank sidecar) | LPJG-version held constant |
| **T3** (convert engine output to ISIMIP3b-style daily NetCDFs) | `trunk_r13078/` unchanged | 0 LOC trunk | ZERO trunk; ~500 LOC translation layer | HIGH risk (monthly→daily disaggregation distorts IMOGEN signal) |

### 1.1 User-refined alternative emerged at session 8.0 mid-discussion (2026-05-20)

User asked: "can we bring trunk_r13078 into the rebuild project as a sibling directory + apply only minimal changes to allow it to accept and run with the climate output produced by the new IMOGEN engine in our rebuild, with engine and LPJG running as **standalones in sequence** (engine completes 1900-2100 first; LPJG then runs against pre-baked library)?"

This is essentially **a sequential-standalone refinement of T2** that drops out several T1/T2 complexities:
- No per-rank sidecar (engine has already finished; LPJG-on-cluster reads pre-baked library)
- No `imogenoutput.cpp` (LPJG-side handshake-writer never fires; no engine waiting)
- No `climatemodel.cpp` engine-side delta in trunk (trunk's `climatemodel.cpp::RUN_IMOGEN_ENGINE` is never executed; skip-engine flag bypasses it)
- No Fortran backport (trunk doesn't have its own Fortran tree; rebuild's engine ran standalone with all its Fortran-side fixes already in place)

This is named **Option T_seq** at session 8.0 C5.

---

## 2. C0–C4 source-diff evidence (paste-back artifacts referenced)

### 2.1 C0 — engine output format compatibility

| Per-year ASCII produced by rebuild engine | Trunk reads (pre-T_seq) | Rebuild reads | Required for T_seq |
|---|---|---|---|
| `T_anom.dat`, `P_anom.dat`, `SW_anom.dat`, `DTEMP_anom.dat`, `WET.dat` | ✅ via existing `gen_filename(file_temp, YYYY, false)` + `read_lines_from_file` pattern | ✅ | configure `.ins` only; ZERO source-edit |
| `CO2.dat` (8-column: year + ppm + 4 dump + change + CH4 + N2O) | ⚠️ trunk reads CO2 via `co2.load_file(filename)` (multi-year LookupTable format); per-year 1-line file may not parse cleanly | ✅ per `climatemodel.cpp:521` (`co2File >> yrCo2File >> co2Ppmv >> dump >> dump >> dump >> co2ChangePpmv >> ch4Ppbv >> n2oPpbv`) | ~30-50 LOC per-year reader in trunk's `imogencfx::readenv()` (option-α per session 8.0 C5 sub-decision) |
| `Rh_anom.dat`, `W_anom.dat`, `Tmin_anom.dat`, `Tmax_anom.dat` (the B4 8-field extension) | ❌ trunk has `file_relhum` + `file_wind` declared at `imogen_input.h:180-181` (skeleton only; missing `file_tmin` + `file_tmax` + missing the `imogencfx` side entirely) | ✅ rebuild post-step-9.5-B4 wiring | ~225-275 LOC B4 wiring backport in `imogen_input.{cpp,h}` + ~100-150 LOC in `imogencfx.{cpp,h}` |

**C0 verdict**: trunk's `gen_filename(...,YYYY,...)` per-year ASCII machinery already exists; the engine library directory structure at `runs/<SCEN>/Common-directory/IMOGEN/output/<year>/` matches exactly; the B4 8-field extension is the bulk of the C0 gap.

### 2.2 C1 — actual backport delta size (vs the preliminary "~150-300 LOC" estimate)

| File | Trunk LOC | Rebuild LOC | Full-fork-parity delta (+/-) | T_seq-relevant delta |
|---|---:|---:|---|---|
| `imogencfx.cpp` | 1105 | 1774 | +683 / -14 | ~100-180 LOC (skip-engine flag + B4 8-field + per-year CO2 reader; year_outer scaffolding ~400 LOC excluded under T_seq) |
| `imogencfx.h` | 241 | 359 | +118 / 0 | ~30 LOC (declarations for the above) |
| `imogen_input.cpp` | 936 | 1574 | +642 / -4 | ~80-100 LOC (symmetric B4 for `-input imogen` consumer; OPTIONAL for T_seq if Track 2 uses only `-input imogencfx`) |
| `imogen_input.h` | 233 | 403 | +172 / -2 | ~25-30 LOC (symmetric B4 declarations) |
| `climatemodel.cpp` | 3319 | 3522 | +233 / -30 | 0 LOC (engine-side; `RUN_IMOGEN_ENGINE` never executed in T_seq) |
| `climatemodel.h` | 254 | 254 | 0 / 0 | 0 LOC (byte-identical) |
| `imogenlogger.{cpp,h}` | 212 | 211 | +4 / -5 | 0 LOC (trivially identical) |
| `cfxinput.{cpp,h}` | 2497 | 2497 | +2 / -2 | 0 LOC (Track 1 path; unchanged) |
| **`imogenoutput.cpp`** | — | 599 | +599 NEW | 0 LOC (LPJG main loop never reaches handshake-writer in T_seq) |
| **`imogenoutput.h`** | — | 222 | +222 NEW | 0 LOC (same) |
| **Fortran `imogen_lpjg.f`** | 4138 canonical | 4700 rebuild | +562 LOC | 0 LOC (trunk has no Fortran; rebuild's engine ran standalone with all its Fortran fixes) |
| **Bash launcher** `mpi_run_guess_on_tmp.sh` | 82 LOC | n/a | up to +30-50 (per-rank sidecar under T1) | 0 LOC (no per-rank sidecar in T_seq; engine already done) |
| **TOTAL** | — | — | **~3300+ LOC full-fork-parity (T_full)** | **~180-310 LOC T_seq minimum-viable (excluding optional symmetric `-input imogen` ~80-100 LOC)** |

**C1 verdict**: T_seq's source-edit scope is ~180-310 LOC, **~5-10× smaller than T1/T2 (~1300-1600 LOC), ~10-15× smaller than T_full (~3300+ LOC)**. The session-7-close "~150-300 LOC" preliminary estimate is actually a reasonable T_seq estimate but was incorrectly applied to T1/T2 framing.

### 2.3 C2 — fix-equivalence in trunk

| Audit item | Source location | Trunk has it? | Required for T_seq? |
|---|---|---|---|
| **B19 Phase 0** SPINUP/FIRSTCALL fix at `imogenoutput.cpp:399-401` | `imogenoutput.cpp` (rebuild-only file) | ❌ N/A — file doesn't exist in trunk | ❌ **No** — LPJG main loop never reaches handshake-writer under T_seq's standalone-LPJG-consumer workflow |
| **B33(c)** Fortran `WARN_POSIX_CONCAT_COLLAPSE` (+145 LOC) | `imogen/code/imogen_lpjg.f` | ❌ NOT in canonical Fortran | ❌ **No** — rebuild's engine (which has this protection) is what runs in T_seq; trunk's LPJG-side never touches the FILE_LPJG_FLUX paths |
| **B17(b)** spinup_year_idx 4-LOC formula correction | `imogen_input.cpp` + `imogencfx.cpp` | ⚠️ trunk has variable + similar structure; the *fix* isn't applied | ⚠️ **Probably not** — only relevant under year_outer mode + tight coupling; T_seq uses gridcell_outer + LPJG-only-consumer |
| **B45** hardcoded year sentinels (`1900/1901/2100/1871`) at `climatemodel.cpp::updateImogenControlData()` line 978 | both | ✅ same brittleness in trunk | ❌ **No** — trunk's `climatemodel.cpp::updateImogenControlData` never runs (no in-process engine) |
| **B36** (audit verdict; zero source change) | n/a | ✅ trivially | n/a |
| **B39** (3 `*_INIT_*` `.ins` values: CO2 296.1, CH4 875.6, N2O 277.4 for YEAR1=1900) | engine-side `imogen_intermediary.ins` | trunk doesn't have its own; rebuild's `runs/SSP1-2.6/imogen_intermediary.ins` is what feeds the engine in T_seq | ❌ N/A — engine-side; rebuild handles it |
| **B40** (audit verdict + paper paragraph) | n/a | ✅ trivially | n/a |
| **B12/B13/B14** (`.ins` parity audit + `ifcalccton`/`ifcalcsla` hardening) | rebuild `runs/<SCEN>/*.ins` | trunk's `.ins` set per `lpjg_landsymm_integration/integrated-4.1-ins2_landsymm_*/` is the predecessor's working production-grade set the user successfully used for Track 1 | ⚠️ ensure the trunk-Track-2 `.ins` set has `ifcalccton 1` + `ifcalcsla 1` (B12 fix); minimal risk because predecessor `.ins` was the canonical baseline that B12 fixed against |

**C2 verdict**: T_seq sidesteps most of the B-series engine-side fixes by virtue of its standalone-LPJG-consumer architecture. The only audit item that traces directly through T_seq's source-edit surface is B12's `ifcalccton 1` + `ifcalcsla 1` setting in the trunk-Track-2 `.ins` files (mechanical apply at block 8.0.2).

### 2.4 C3 — legacy `owl_hpc_cluster_scripts/scripts/` launcher pattern

| Aspect | Finding | T_seq impact |
|---|---|---|
| `mpi_run_guess_on_tmp.sh` (per-rank wrapper; 82 LOC) | clean single `${GUESS} ${OPTIONS}` invocation at line 62 | ✅ works as-is for trunk's LPJG-only invocations |
| `setup_run_owl_with_scratch_lpj_work.sh` (orchestrator; 175 LOC) | passes `-input $INPUTMETHOD` (line 155); generates `submit.sh` SLURM script + `startguess.sh`; mature pattern | ✅ change `INPUTMETHOD=cfx` (Track 1) → `INPUTMETHOD=imogencfx` (Track 2); single-arg change |
| Per-rank engine duplication concern | each rank would run own engine if T1 chosen (~6 hours wasted on 32 ranks × 5 SSPs) | ✅ N/A under T_seq — no per-rank engine; pre-baked library SCP'd to cluster once |
| SCP burden | rebuild engine output library ~50 MB per SSP × 5 SSPs ≈ 250 MB (NOT 50 GB; that was an over-estimate — per `runs/SSP1-2.6/Common-directory/IMOGEN/output/2000/` U3 inspection: 246 KB × 10 files × 201 years = ~500 MB per SSP × 5 = ~2.5 GB total; manageable single one-time SCP) | ✅ minor — `scp -r` of 5 directories or `rsync` |
| `make_guess.sh` (build) | clean MPI build pattern; reusable for both forks | ✅ adapt for `forks/trunk_r13078/build_trunk/` build dir |

**C3 verdict**: legacy launcher supports T_seq cleanly. No per-rank sidecar orchestration required.

### 2.5 C4 — effort estimate comparison

| Option | Backport LOC | Verification | Calendar wall | v1.0 schedule impact |
|---|---:|---|---|---|
| **R** | 0 | 0 | 0 | 0 weeks added |
| **Hybrid** (R + paper Methods paragraph + ε-bound calibration cell) | 0 | ~2-3 d | ~0.5 week | +0.5 week |
| **T_seq** ✅ ADOPTED at C5 | ~180-310 LOC C++ (minimum-viable) + ~150-300 LOC `.ins` + ~50 LOC build doc | local engine standalone → trunk standalone 4-cell test → 8-acceptance-gate evaluation | ~2-3 working days | +0.5-1 week |
| **T1_min** | ~1300-1600 LOC | wider | ~1-1.5 weeks | +1-2 weeks |
| **T2_min** | ~1350-1650 LOC | wider | ~1-1.5 weeks | +1-2 weeks |
| **T_full** | ~3300+ LOC | full xval | ~2-3 weeks | +2-3 weeks |

**C4 verdict**: T_seq is the **best-of-both-worlds** option: preserves paper-reviewer-defensibility (LPJG version held constant between Track 1 and Track 2) at a fraction of the T1/T2/T_full cost.

### 2.6 C5 decision (this commit)

✅ **Option T_seq adopted** per session 8.0 user authorisation at 2026-05-20 afternoon.

Sub-decisions:
- **Landing path**: `forks/trunk_r13078/` (cleaner organization for the dual-fork lifestyle per `TRUNK_R13078_BACKPORT_LEDGER.md` §1.1)
- **CO2 bridge**: option-α (in-source per-year CO2.dat reader; ~30-50 LOC trunk source-edit; matches existing T_anom.dat pattern) — preferred over option-β Python aggregator for source-side cleanliness

---

## 3. U1–U4 pre-flight verification outcomes

### 3.1 U1 — `read_lines_from_file` machinery parity

✅ **IDENTICAL signatures in both trunk and rebuild**:
- `IMOGENCFXInput::read_lines_from_file(xtring fname, std::vector<double> lons, std::vector<double> lats, std::vector<int> lines, std::vector<std::vector<double> >& data, bool monthly)` — trunk line 707; rebuild line 793
- `ImogenInput::read_lines_from_file(xtring fname, std::vector<double> lons, std::vector<double> lats, std::vector<int> lines, std::vector<std::vector<double> >& data, bool monthly)` — trunk line 535; rebuild line 592

Same `std::ifstream(fname, std::ifstream::in)` machinery. **Backport for B4 8-field extension is pure mechanical line-by-line addition** of new `gen_filename(file_<var>, calendar_year, false)` + `read_lines_from_file(filename, lons, lats, line_index, all_<var>[store_index], monthly)` blocks in `readenv()` + new storage `std::vector<std::vector<std::vector<double>>>` members + new `climate.<var> = all_<var>[store_index];` assignment lines in `get_climate_for_gridcell()`.

### 3.2 U2 — `exit(200)` removal scope

✅ **Trivial 1-line delete** at `version_A/.../trunk_r13078/modules/imogencfx.cpp:483`:

```cpp
482: 	RUN_IMOGEN_ENGINE();
483: 	exit(200);          // DELETE THIS LINE
484:
485: 	// allways allow space from 1st spinup year
```

Combined with the skip-engine flag wrap, the surgical edit at lines 480-490 becomes:

```cpp
// post-T_seq:
if (!IMOGENConfig::skip_inprocess_engine_run) {
    RUN_IMOGEN_ENGINE();
} else {
    dprintf("%s: skip_inprocess_engine_run=true; skipping in-process RUN_IMOGEN_ENGINE() call. "
            "User must have pre-staged climate at <DIR_COMMON>/IMOGEN/output/<YYYY>/. See notes/B47.md.\n",
            __FUNCTION__);
}
// (exit(200) deleted)

// allways allow space from 1st spinup year
```

~10 LOC net change.

### 3.3 U3 — engine output library structure vs trunk's filename expectations

✅ **Exact match**. Rebuild's B44 acceptance-test library at `runs/SSP1-2.6/Common-directory/IMOGEN/output/<year>/` contains:

```
CO2.dat           65 bytes (1 line; 8 columns)
T_anom.dat        246 KB
P_anom.dat        246 KB
SW_anom.dat       246 KB
DTEMP_anom.dat    246 KB
Rh_anom.dat       246 KB  (B4 8-field addition)
W_anom.dat        246 KB  (B4)
Tmin_anom.dat     246 KB  (B4)
Tmax_anom.dat     246 KB  (B4)
WET.dat           246 KB
done              22 bytes  (sidecar marker; ignored by LPJG)
dtemp_o.dat       2.8 KB    (engine internal; ignored)
fa_ocean.dat      40 KB     (engine internal; ignored)
```

Trunk's `gen_filename(file_<var>, YYYY, false)` pattern at line 129 substitutes `YYYY` in the configured path with `calendar_year`. Per trunk's comment at line 105: `param "file_temp" (str "/scratch/.../imogen/YYYY/T_anom.dat")` — the rebuild's library directory at `<DIR_COMMON>/IMOGEN/output/<year>/<filename>.dat` matches this pattern exactly. Trunk-Track-2 `.ins` configuration: `file_temp = "<DIR_COMMON>/IMOGEN/output/YYYY/T_anom.dat"` etc.

### 3.4 U4 — mutual-exclusion + coupling_mode concerns

✅ **No new concerns for T_seq**:
- Trunk HAS `FILE_LPJG_FLUX` + `FILE_LPJG_CH4_N2O_FLUX` + `FILE_SCEN_EMITS` + `FILE_CH4_N2O_EMITS` parameters declared (lines 218-223 of `imogencfx.cpp`) — but these are engine-side `.ins` parameters that the engine consumed when it ran standalone in the rebuild repo; trunk's LPJG-runs-against-pre-baked-library workflow leaves them syntactically declared but functionally unused. Set to empty string `""` in trunk-Track-2 `.ins`.
- Trunk does NOT have `coupling_mode` parameter (rebuild added at step 8). Doesn't matter for T_seq because `imogenoutput.cpp` doesn't exist in trunk + LPJG main loop never reaches the handshake-writer code path.
- B19/B33 defense-in-depth layers (launcher auto-rewrite + launcher pre-flight + Fortran defensive PRINT) are all engine-side; the rebuild's engine (which has them) runs standalone in T_seq's Step A; trunk's Step B is pure LPJG-consumer with no engine interaction.

---

## 4. T_seq implementation plan (blocks 8.0.1-8.0.3)

### 4.1 Block 8.0.1 — Structural import of trunk_r13078 into rebuild repo

**Scope**: Import `version_A/LPJG-IMOGEN-COUPLED-MODEL-FRAMEWORK/Integrations/trunk/trunk_r13078/` into rebuild repo as `forks/trunk_r13078/`. Single structural commit; ZERO source-edit.

**Steps**:
1. `mkdir -p forks/`
2. `rsync -a --exclude='build*' --exclude='*.o' --exclude='guess' --exclude='*.log' --exclude='.git' version_A/.../trunk_r13078/ forks/trunk_r13078/`
3. Add `forks/trunk_r13078/build*/` to `.gitignore`
4. Add `forks/README.md` documenting the two-fork policy (cross-reference `TRUNK_R13078_BACKPORT_LEDGER.md` §1.1)
5. Verify the imported tree compiles cleanly: `cd forks/trunk_r13078 && mkdir build && cd build && cmake .. && make` — produces working `guess` binary (pre-T_seq-source-edit baseline)
6. Add 6-surface doc cascade entries documenting the structural commit
7. Commit + 3-remote push

**Acceptance**:
- Standalone trunk build produces `forks/trunk_r13078/build/guess` binary
- Baseline `cfx` 4-cell smoke run passes (Track 1 reproducibility on imported trunk; ensures the import is clean)
- 3-remote convergence verified

**Effort**: ~0.5 day. Mechanical structural work.

### 4.2 Block 8.0.2 — T_seq source-edit + new trunk-Track-2 `.ins` set

**Scope**: Apply ~180-310 LOC C++ source-edit to `forks/trunk_r13078/modules/` + author new trunk-Track-2 `.ins` files at `forks/trunk_r13078_runs/SSP*/` (or similar; structure-decision TBD at 8.0.2 start).

**Surgical sites in `forks/trunk_r13078/modules/imogencfx.cpp`**:
1. Add `IMOGENConfig::skip_inprocess_engine_run` declaration (~5 LOC; matches rebuild line 365)
2. Add `declare_parameter("skip_inprocess_engine_run", ...)` in `init()` (~3 LOC)
3. Wrap `RUN_IMOGEN_ENGINE();` at line 482 in `if (!skip)` else dprintf (~10 LOC); delete `exit(200);` at line 483
4. Add `file_relhum` + `file_wind` + `file_tmin` + `file_tmax` parameter declarations + storage members (~30 LOC)
5. Extend `readenv()` to read Rh/W/Tmin/Tmax per-year (~60 LOC; 4 new gen_filename + read_lines_from_file blocks)
6. Extend `get_climate_for_gridcell()` to copy Rh/W/Tmin/Tmax into climate object (~40 LOC)
7. Add per-year CO2.dat reader (option-α; ~30-50 LOC) — bespoke since engine writes 1-line 8-column format

**Surgical sites in `forks/trunk_r13078/modules/imogencfx.h`**:
1. Add `skip_inprocess_engine_run` to `IMOGENConfig` (~3 LOC)
2. Add `file_relhum` + `file_wind` + `file_tmin` + `file_tmax` private members (~8 LOC)
3. Add `all_drelhum` + `all_dwind` + `all_dtmin` + `all_dtmax` storage member declarations (~15 LOC)

**Optional symmetric backport in `forks/trunk_r13078/modules/imogen_input.{cpp,h}`** (~80-100 LOC; only if Track 2 also wants `-input imogen` support; T_seq Track 2 default is `-input imogencfx`, so this is OPTIONAL; defer to v1.1+ if not needed).

**New `.ins` files** at `forks/trunk_r13078_runs/SSP1-2.6/main.ins` (adapt from `lpjg_landsymm_integration/integrated-4.1-ins2_landsymm_hist/main.ins`; ~150-300 LOC mechanical):
- `INPUTMETHOD imogencfx` (was `cfx` in Track 1)
- `skip_inprocess_engine_run 1` (NEW T_seq parameter)
- `DIR_COMMON "<path-to-engine-library>"` (points at `runs/SSP1-2.6/Common-directory/`)
- `file_temp "<DIR_COMMON>/IMOGEN/output/YYYY/T_anom.dat"` (per-year ASCII)
- `file_prec "<DIR_COMMON>/IMOGEN/output/YYYY/P_anom.dat"` etc.
- `file_relhum "<DIR_COMMON>/IMOGEN/output/YYYY/Rh_anom.dat"` (NEW)
- `file_wind "<DIR_COMMON>/IMOGEN/output/YYYY/W_anom.dat"` (NEW)
- `file_tmin "<DIR_COMMON>/IMOGEN/output/YYYY/Tmin_anom.dat"` (NEW)
- `file_tmax "<DIR_COMMON>/IMOGEN/output/YYYY/Tmax_anom.dat"` (NEW)
- `file_co2 "<DIR_COMMON>/IMOGEN/output/YYYY/CO2.dat"` (per-year)
- All 4 FILE_LPJG_*/FILE_*_EMITS set to empty `""` (engine-side; N/A in T_seq)
- `ifcalccton 1` + `ifcalcsla 1` (B12 defensive)
- Production parameters from `notes/PRODUCTION_RUN_CONFIG.md` §2.1 (`nyear_spinup 500`, `npatch 25`, `firemodel BLAZE`, `file_simfire`, popdens, ndep NetCDFs, `_peatland` LU)

**Build verification**:
- `cd forks/trunk_r13078 && cd build && make` — clean build with T_seq source-edits
- Local 4-cell smoke run: rebuild engine `scripts/run_coupled.sh --engine-only-mode` → produces `runs/SSP1-2.6/Common-directory/IMOGEN/output/<year>/` library → `forks/trunk_r13078/build/guess -input imogencfx main.ins` reads pre-baked library → produces LPJG ecosystem outputs

**Effort**: ~1-1.5 days.

### 4.3 Block 8.0.3 — Acceptance test + audit-evidence bundle

**Scope**: Local 4-cell smoke run of T_seq end-to-end (engine standalone → SCP-equivalent local copy → trunk standalone LPJG) with 8-acceptance-gate evaluation per Rule #10.

**8 acceptance gates** (per CLUSTER_SETUP §7.1 pattern):

| Gate | Criterion | Verification method |
|---|---|---|
| G0 | trunk-T_seq build sha1-stable | `sha1sum forks/trunk_r13078/build/guess` |
| G1 | git HEAD-stable | `git log -1 --format=%H` |
| G2 | Engine library produced by rebuild `--engine-only-mode` (202 year-dirs 1900-2101) | `ls runs/SSP1-2.6/Common-directory/IMOGEN/output/ \| wc -l` |
| G3 | All 4 cells processed by trunk-T_seq | LPJG `*.out` files have 4 cell-rows per year |
| G4 | No ERROR/SEVERE/FATAL in either log | `grep -icE 'ERROR\|SEVERE\|FATAL\|abort' logs/*` |
| G5 | 8-field climate consumed correctly | `dprintf` from trunk readenv showing all 8 file_<var> read successfully + climate.relhum / climate.u10 / climate.tmin / climate.tmax non-NaN |
| G6 | Per-year CO2 consumed correctly | engine CO2.dat[1900]=295.844 → climate.co2 at year 1900 in trunk matches |
| G7 | LPJG ecosystem outputs sanity-bounded | cflux / cmass / mch4 / ngases within order-of-magnitude of Track 1 cfx baseline on same 4 cells (not bit-exact; this is climate-driver-comparison) |

**Audit-evidence bundle**: `_chat_artifacts/b47_tseq_acceptance_4cell_2026-05-20/`:
- Engine run log (~7 MB; from rebuild `--engine-only-mode`)
- Trunk LPJG run log
- Selected `*.out` files (cflux, mch4, ngases, ndep, landcover)
- 8-gate acceptance evaluation Markdown
- T_seq design diff for reference

**Effort**: ~0.5-1 day.

---

## 5. Cumulative backport-debt accounting under T_seq

**Under Option R (prior default)**: cumulative TRUNK-RELEVANT backport-debt was +145 LOC (entirely from B19 Phase 2 Commit 3 `6862d03`'s `imogen/code/imogen_lpjg.f::WARN_POSIX_CONCAT_COLLAPSE`); to be replicated to `trunk_r13078` (which was external to the rebuild repo at session-7 close) in post-paper Backport Sprint per ledger §1.2.

**Under Option T_seq (THIS commit)**: the framing refines into a **two-installment backport trajectory**:

**Installment 1 — T_seq first-installment** (blocks 8.0.1-8.0.3; pre-paper Track 2 enablement):
- ~180-310 LOC C++ source-edit to `forks/trunk_r13078/modules/imogencfx.{cpp,h}` (skip-engine flag + B4 8-field consumer wiring + per-year CO2.dat reader)
- 1-line `exit(200)` regression removal at `imogencfx.cpp:483` (matches LandSyMM_LPJ-GUESS state per ledger §2 → reduces 6-file baseline-diff to 5-file)
- Classification: **TRUNK-RELEVANT to `forks/trunk_r13078/`** (in-repo from block 8.0.1 onwards)
- Purpose: bare-minimum Track-2-runnable state for paper-publication runs

**Installment 2 — Post-paper Backport Sprint** (per ledger §1.2 policy; UNCHANGED in framing, deferred timing):
- Full forward-port of remaining ~2900-3100 LOC of rebuild deltas to `forks/trunk_r13078/`:
  - `imogencfx.cpp` year_outer scaffolding (~400 LOC: `preload_all_climate` + `getclimate_for_year`)
  - `imogenoutput.cpp` + `imogenoutput.h` (~821 LOC NEW; LPJG-side handshake-writer for v1.1+ tight coupling)
  - `climatemodel.cpp` engine-side delta (~263 LOC: B19 SPINUP/FIRSTCALL fix + B17(b) 4-LOC spinup_year_idx + Tmin/Tmax/Rh/W engine writers + B45 hardcoded year sentinels if fixed)
  - `framework.cpp` year_outer code path (~50-100 LOC)
  - Fortran `imogen_lpjg.f` ~562 LOC (step-3 ALLOCATABLE refactor + B10 alternating-year fix + B1/B2 Rh/W/Tmin/Tmax engine writers + B33(c) WARN_POSIX_CONCAT_COLLAPSE +145 LOC)
- Purpose: bring `forks/trunk_r13078/` to **full fork-parity** with `lpjguess/` so both forks are **switchable alternatives** per the two-fork long-term trajectory (§0 of this document)
- Timing: post-paper Backport Sprint era (post-step-19; matches original ledger §1.2 schedule)
- Eligible-for-backport tracking: this installment's LOC accounting accumulates incrementally as each rebuild engine-side or LPJG-side change lands in `lpjguess/`

**Rebuild-side backport-debt to LANDSYMM_LPJ-GUESS upstream (the canonical-Fortran-engine direction; original ledger §1.2 framing)**: UNCHANGED at +145 LOC (still applies to canonical Fortran engine at `version_A/.../IMOGEN-codebase/code/imogen_lpjg.f` upstream; deferred to post-paper sprint; Option T_seq doesn't preclude this future work; rebuild's engine at `lpj-guess_imogen_landsymm/imogen/code/imogen_lpjg.f` is what runs in T_seq Step A so the +145 LOC protection IS exercised in T_seq runs).

**Ledger §1.2 workflow refinement under T_seq**:
- The "Backport Sprint at end of Phase-1 (post-step-19)" framing in §1.2 now covers TWO directions:
  1. Rebuild → LANDSYMM_LPJ-GUESS-upstream (canonical Fortran engine; +145 LOC; original direction)
  2. Rebuild's `lpjguess/` → `forks/trunk_r13078/` Installment-2 full-parity port (~2900-3100 LOC; NEW under T_seq dual-fork trajectory)
- The T_seq Installment-1 edits at blocks 8.0.1-8.0.3 are a NEW pre-paper class: bare-minimum forward-port for Track 2 reviewer-defensibility, tracked separately from the post-paper Installment-2 work.
- Cumulative running ledger henceforth tracks both installments separately so the two-fork-parity completion progress is auditable.

---

## 6. Operational heuristics datapoints

**Rule #9 (harness-authoring + targeted verification routinely surfaces latent defects)** — 14th consecutive recurrence: the C0-C4 source-diff exercise + U1-U4 pre-flight verification surfaced (a) the over-optimism of the session-7-close "~150-300 LOC" preliminary estimate (5-10× under-scoped when interpreted as T1/T2; just-right when interpreted as T_seq); (b) the user-refined sequential-standalone framing (T_seq) which wasn't in the original 4-options-list at CLUSTER_SETUP §0.2 but cleanly drops out the bulk of T1/T2 scope; (c) the **Hybrid** alternative (R + paper Methods paragraph + ε-bound demonstration) that wasn't in any prior framing; (d) U3's empirical confirmation that engine library structure exactly matches trunk's existing `gen_filename(...,YYYY,...)` pattern — no path-rewrite needed.

**Rule #10 (verification-integrity discipline)** — 14th consecutive clean operating datapoint with **1 honest self-correction**: the C1 LOC table at session-8.0-C1 evidence presentation surfaced that the session-7-close preliminary "~150-300 LOC; few-hour to ~1-day" T-estimate was over-optimistic by ~5-10× when interpreted as in-process T1/T2 framing. Per Rule #10's amendment-vs-rewrite corollary, the original session-7-close estimate is preserved at `CLUSTER_SETUP §0.2` + `TRUNK_R13078_BACKPORT_LEDGER` strategic note as historical context (it encodes what was AIMED for); the post-C0-C4 honest re-baselining is preserved here in this B47 record + propagates to the cascade docs (it encodes what the empirical scope IS). User's T_seq refinement then brings the actual implementation scope back down close to the session-7-close estimate — a clean Rule-#10 episode where preliminary intuition + honest mid-course re-baselining converged on a viable plan.

---

## 7. What's next (post-B47 / session 8 continuation)

| Order | Item | Estimated effort | Priority |
|---|---|---|---|
| **8.0.1** | Structural import of `forks/trunk_r13078/` into rebuild repo + baseline build verification + 3-remote push | ~0.5 d | HIGH (T_seq prerequisite) |
| **8.0.2** | Apply ~180-310 LOC source-edit to `forks/trunk_r13078/modules/` + author trunk-Track-2 `.ins` files + build verification | ~1-1.5 d | HIGH (T_seq core) |
| **8.0.3** | Local 4-cell smoke acceptance test (rebuild engine → trunk LPJG) + 8-acceptance-gate evaluation + audit-evidence bundle + 3-remote push | ~0.5-1 d | HIGH (T_seq close) |
| **8.1** | Cluster reconnaissance on KIT IMK-IFU `owl` — `env_owl.sh` + sinfo + sacct + storage paths (SSH paste-back); now ALSO covers trunk-build module dependencies for `forks/trunk_r13078/build_owl/` | ~30-60 min SSH-iterative | HIGH |
| **8.2** | User's LPJG-on-owl workflow walkthrough; informs both `lpj-guess_imogen_landsymm/scripts/cluster/` AND `forks/trunk_r13078/` cluster adaptation | ~30 min | HIGH |
| **8.3** | `-input imogencfx` source-read in trunk; verify T_seq cluster integration approach (probably: SCP engine library to cluster; run trunk-LPJG-only with `-input imogencfx`; no in-process engine) | ~30 min | HIGH |
| **8.4** | Local production-config delta authoring per `notes/PRODUCTION_RUN_CONFIG.md` §2 — targets `forks/trunk_r13078_runs/SSP1-2.6/` under T_seq | ~2-3 h | HIGH |
| **8.5** | Local 100-cell production test run for trunk-T_seq with full production knobs (BLAZE, popdens, ndep, _peatland LU); 8-acceptance-gate evaluation | ~1-2 h | HIGH |
| **8.6** | Local Track-1 paired 100-cell sample run with trunk's `-input cfx` for validation triad comparison | ~1-2 h | HIGH |
| **8.7** | Cluster sbatch wrapper update for trunk-T_seq cluster runs | ~1-2 h | HIGH |
| **8.8** | Cluster setup decision point + commit before SCP/cluster scaling | ~discussion | HIGH |
| sessions 9-11 | Cluster Track 2 production runs (5 SSP-RCPs × 1900-2100 × 62538 cells; trunk-T_seq on owl) | ~2-4 weeks | HIGH (paper-publication critical-path) |
| sessions 11-12 | Validation triad + paper figures | ~1 week | HIGH |
| sessions 12-15+ | Paper amendments + writing | ~2-3 weeks | HIGH |
| sessions 15+ | v1.0 paper submission to GMD | terminal | TERMINAL |

**Total estimated calendar time from this commit to v1.0 paper submission**: **~6-11 weeks** (revised from ~6-10 weeks at session-7-close by adding T_seq's ~0.5-1 week to the schedule; still within the original budget envelope).

---

## 8. Cross-references summary

- `notes/CLUSTER_SETUP_AND_PRODUCTION_RUNS.md` §0.2 (session 8.0 strategic question + this B47 outcome resolves it)
- `notes/TRUNK_R13078_BACKPORT_LEDGER.md` strategic note (direction-inversion now confirmed under T_seq; cumulative-debt accounting updated)
- `notes/PAPER_COMPLETION_AND_VALIDATION.md` §1.4 (Axis 4 validation interpretation; T_seq resolves LPJG-version-confounding concern)
- `notes/B44.md` §3 (rebuild's `--engine-only-mode` flag; the productisation T_seq depends on for Step A engine-standalone run)
- `notes/B37.md` §5 (path-iv `done`-marker sidecar mechanism that B44 productised; engine produces 202 year-dirs 1900-2101 in ~12.5 min on smoke 4-cell)
- `notes/FOLLOWUPS.md` B-series (all engine-side fixes inherit into T_seq via rebuild's standalone engine run; no additional trunk-side fix needed for B19/B33/B36/B39/B40)
- `notes/LOCAL_V1_VERIFICATION_WINDOW.md` (engine-side validation already complete at `429f723`; T_seq leverages this)
- `lpj-guess_imogen_landsymm/scripts/run_coupled.sh` (rebuild launcher with `--engine-only-mode`; T_seq Step A)
- `forks/trunk_r13078/` (NEW post-8.0.1; the T_seq runs surface for paper-publication Track 2)
- `forks/trunk_r13078_runs/SSP*/` (NEW post-8.0.2; trunk-Track-2 `.ins` files)
- `lpjg_landsymm_integration/integrated-4.1-ins2_landsymm_{hist,ssp126}/` (existing predecessor production-style `.ins` files for `-input cfx` Track 1; adapted to trunk-T_seq Track 2 at block 8.0.2)
- `owl_hpc_cluster_scripts/scripts/` (legacy cluster launcher pattern; reused as-is for trunk-T_seq cluster runs post-block-8.7)
- `_chat_artifacts/b47_tseq_decision_2026-05-20/` (audit-evidence bundle for this decision commit; pre-flight verification logs + C0-C4 + U1-U4 source-diff outputs)
- `_chat_artifacts/CHAT_HANDOFF_2026-05-18_session5_post_b19.md` Part 9 (session-8 narrative; NEW at this commit)
- `EXECUTION_PLAN.md` row 17c (status update: T_seq decision at session 8.0; 8.0.1-8.0.3 implementation blocks ACTIVE NEXT)
- `CHANGELOG.md` `[Unreleased]` (dated entry for this commit)
- `notes/STEP_17c.md` §1.7.8 (post-T_seq update; 17c.1+ cluster phases now target `forks/trunk_r13078/`)

---

_End of `notes/B47.md` — B47 Option T_seq strategic decision ✅ DONE 2026-05-20 afternoon session 8.0 opening._
