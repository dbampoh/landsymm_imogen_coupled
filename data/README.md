# `data/` — reference data

Small reference data is committed to git. Large data (gridded LU,
gridded climate, gigabyte-scale Lamarque archives, etc.) is **not
committed** but is documented in `data/DATA.md` so any user /
collaborator / successor knows how to obtain it. Step 6 of the rebuild
plan populated this directory.

## Layout (post-step-6)

```
data/
├── DATA.md                               (NEW step-6) Acquisition recipes for
│                                          all Tier-3 large datasets that this
│                                          tree references but does NOT contain.
├── soil/                                 (Tier 1 — committed to git, 3.5 MB)
│   └── soilmap_center_interpolated.remapv10_old_62892_gL.dat
├── gridlist/                             (Tier 1 — committed, 3.4 MB, 7 files)
│   ├── gridlist_in_62892_and_climate.txt The active full 62 538-cell
│   │                                      gridlist for production runs.
│   ├── gridlist_test2.txt                4-cell smoke-test gridlist.
│   ├── gridlist_hilda+.txt               62 864-cell HILDA+-style.
│   ├── gridlist_hurtt_RNDM_midpoint.txt  Hurtt-style 59 191-cell.
│   ├── gridlist_hurtt_RNDM_midpoint_3698.txt   Reduced 3 696-cell variant.
│   ├── ndep_gridlist.txt                 N-deposition support gridlist.
│   └── patterns_gridlist.txt             1631-cell IMOGEN HadCM3 grid.
├── concentrations/                       (Tier 1 — committed, 7.5 MB)
│   ├── EPA/                                3 CSVs (CO2/CH4/N2O concentrations)
│   ├── IIASA/CMIP5/                       2 interpolated *.txt + TXT/ subdir
│   │   └── TXT/                            6 .DAT (MAGICC mid-yr) + 12 *.txt
│   │                                       (rcp{26,45,60,85}_{co2,ch4,n2o}_interpolated)
│   ├── IIASA/CMIP6/                       (raw XLSX/ZIP downloads excluded;
│   │   └── XLSX/                            see DATA.md "IIASA CMIP6")
│   └── (raw IIASA XLSX/ZIP downloads excluded; see DATA.md)
├── ndep/                                 (Tier 2 — gitignored; ~501 MB)
│   ├── README.md                           Acquisition pointer
│   └── ndep_cruncep/                       (5 .bin files; fetch via
│                                            tools/fetch_imogen_data.sh
│                                            --component ndep-lamarque)
└── (planned at later steps:)
    ├── lu/                               step 6.5 / 9 — see DATA.md ("PLUM scenario LU")
    ├── observations/                     step 17 — NOAA GMD + AGAGE for validation
    └── emissions/                        intermediary_py outputs land here at step 12
```

## Tiers

Per `[CMI §5]` and EXECUTION_PLAN.md V.1 step 6, the data is partitioned
into three tiers:

- **Tier 1 — committed directly to git** (~14 MB total):
  `soil/`, `gridlist/`, `concentrations/` (small TXT/CSV/DAT files only).
  Source-of-truth for cell selection, soil properties, reference GHG
  concentration timeseries; small enough that bundling them with the
  source code is the pragmatic choice.

- **Tier 2 — fetch-script + tarball** (regenerated by
  `tools/fetch_imogen_data.sh`):
  - `ndep/ndep_cruncep/` (~501 MB; Lamarque binary fast-archives;
    fetched via `--component ndep-lamarque`)
  - GCM patterns and CRUNCEP at `imogen/patterns/`, `imogen/CRUNCEP_1960_1989/`
    (already fetched at step 4)
  - IMOGEN reference emissions at `imogen/emiss/` (5 MB; fetched via
    `--component emiss-reference`)

- **Tier 3 — document-only** (very large or modeller-supplied;
  acquisition recipes in `data/DATA.md`):
  - PLUM scenario LU at `/media/bampoh-d/lpjg_input/input/LU/plum_harm_lu/`
    (86 GB on user's external drive; per Decision #10 used via
    `save_state`/`restart`)
  - Legacy concatenated `Data/LU/SSP1_RCP26_concatenated/` (7.3 GB;
    superseded by PLUM LU above; kept as fallback for Option C)
  - Legacy `Data/Intermediary/` (2.2 GB; production shifts to
    `intermediary_py` at step 10)
  - Predecessor's pre-baked output `IMOGEN_SSPx_RCPy_Clim/` etc.

## Discipline

Per `EXECUTION_PLAN.md` §I.D.1, every numeric data file in `data/`
carries a units header (or `.meta.yaml` sidecar) declaring columns,
units, sign conventions, spatial and temporal resolution, year
coverage. When adding a new file, declare its schema. When reading
it, validate against the schema.

## Sources

The full source-of-truth table for every input dataset is in
`EXECUTION_PLAN.md` §I.C and the master doc `[CMI §5]`. Highlights:

- **HILDA+ v2** — Winkler et al. 2021 (ESSD) + the user's
  `landsymm_py` harmonisation pipeline.
- **PLUM v2** outputs — delivered by the PLUM modelling team based
  on the user's prior LPJ-GUESS Stage I yield surfaces.
- **FAOSTAT, RCMIP Phase 2 v5.1.0, FAIR ERF v1.3, EDGAR 2025** — public
  downloads (URLs in DATA.md).
- **GCM patterns** — the user's existing `Common-directory/IMOGEN-codebase/patterns/`
  set: 34 CMIP5 ASCII + 1 CMIP3 legacy + 5 CMIP6 NetCDF (converted
  via `tools/cmip6_nc_to_cmip5_ascii.py` to CMIP5 ASCII format the
  Fortran reader consumes; output in `imogen/patterns/CEN_CMIP6_MOD_<gcm>/`).
- **CRUNCEP base climatology** — Viovy 2018; in
  `imogen/CRUNCEP_1960_1989/`.

## Provenance

For reproducibility, `data/DATA.md` will record SHA-256 checksums
and download URLs for each public dataset, and modeller-of-record
for the project-internal datasets (PLUM team, the user's prior
landsymm_py runs, etc.).
